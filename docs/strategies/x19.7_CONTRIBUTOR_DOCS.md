# MS19 Phase 7 Strategy — Contributor Docs Review

Status: Complete.
Parent milestone doc: [`docs/milestones/19_OB_HARDENING_OPS.md`](../milestones/19_OB_HARDENING_OPS.md)

**Goal:** Review and refresh contributor-facing documentation so onboarding into the project is current and accurate before open beta.

## Decisions made

- **Scope:** AGENTS.md, CLAUDE.md, AgentRoles/, and the contributor-facing parts of `docs/ops/`. Don't broaden to README.md or DOC_ROLES.md — those have different shapes (project-overview and doc-tree-role docs) and review on a different cadence. Don't try to neutralize agent docs for hypothetical future contributors; this is a solo-operator project and maintainer-style is the right voice.
- **Retire-or-merge candidates:** Discover during the review pass rather than enumerate up front. If a doc surfaces as stale or redundant during the walk, retire or merge it then.

## 1. Review AGENTS.md

1. [x] Walk every section. Note items that are stale, no longer apply, or contradict current practice. One staleness: the mail-alias Environment Note described aliasing as active post-Phase-1 disable — reworded to mechanism + standing rule; the doc doesn't track toggle state. All other sections current.
2. [x] Add any standing rules that have emerged during recent work but aren't yet documented (project memories that should be in-repo, conventions adopted in conversations, etc.). Five added: no-Sunday-commits, x-prefix completed docs, immediate checkoffs, push-over-cron, specs-state-behavior.
3. [x] Confirm cross-references (DOC_ROLES.md, `.cybercreek/AGENTS_LOCAL.md`, milestone docs) are still accurate. All targets exist; descriptions match (incl. the local-only test tag and the Harvey role example).
4. [x] Keep the maintainer-style framing — don't over-neutralize. No neutralizing edits made.

## 2. Review CLAUDE.md

1. [x] Same pass as AGENTS.md. Keep CLAUDE.md aligned as an addendum, not a divergence. Clean; the one surfaced rule (commit summaries) went to AGENTS.md Working Style instead.
2. [x] Confirm Claude-specific guidance still reflects current behavior and project conventions.
3. [x] Verify the `@AGENTS.md` import line still makes sense. Loads every session.

## 3. Review AgentRoles/

1. [x] List every role doc currently under `AgentRoles/`. One: `HARVEY.md`.
2. [x] For each: still relevant, needs update, or should be retired? Apply per-case. Harvey kept as-is.

## 4. Review contributor-facing parts of docs/ops/

1. [x] List ops docs that contain contributor-facing guidance (vs. operational runbooks for the maintainer alone). Contributor-facing: `get-live/QUICK_START.md`, `get-live/CONTRIBUTOR_WALKTHROUGH.md`, the contributor-outputs half of `DOCS_DX.md`. Maintainer runbook: `OB_ROLLOUT_CHECKLIST.md`.
2. [x] Update stale guidance; flag operational-vs.-contributor sections if a doc mixes both. QUICK_START: `queue` service added to the stack note, host-Node prerequisite dropped (Sail's container carries Node), fresh-clone Composer bootstrap added. Walkthrough clean.

## 5. Retire-or-merge pass

1. [x] Apply any retire/merge decisions surfaced in §1–4. None surfaced.
2. [x] Update cross-references in remaining docs to reflect retirements/merges. Nothing to update.

## Exit Criteria

- [x] AGENTS.md, CLAUDE.md, and AgentRoles/ reviewed; stale guidance updated or removed.
- [x] Contributor-facing portions of `docs/ops/` reviewed.
- [x] Standing rules that emerged during recent work are captured in the right doc.
- [x] Any retire/merge actions completed; cross-references updated. None needed.
