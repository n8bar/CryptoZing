# MS2 Phase 1 Strategy - Platform & Client Foundations

Status: Complete (retrospective reconstruction).
Parent milestone: [`docs/milestones/x2_INVOICE_UX_FOUNDATIONS.md`](../milestones/x2_INVOICE_UX_FOUNDATIONS.md)
Historical execution window: 2025-10-18 through 2025-10-23.

> This phase boundary was reconstructed on 2026-07-18 from a cohesive commit cluster. It was not named or checked off as Phase 2.1 during original execution.

## Goal

Establish the authenticated Laravel/Sail application, its per-user client/invoice domain, and a usable client-management surface before the invoice UI build.

## Reconstruction basis

Primary evidence: `aa0d045`, `40c511b`, PR #1 merge `b8b2274`, `72f6a91`, PR #2 merge `fd6aa5b`, `9f29fe7`, and PR #3 merge `4c33980`.

## 1. Bootstrap the application

1. [x] Add the Laravel application and baseline framework configuration. %<7518>
   1. [x] Include Breeze authentication and profile flows. %<7519>
   2. [x] Include Sail/Compose, MySQL configuration, and frontend build tooling. %<7520>
   3. [x] Include the framework's baseline auth/profile tests. %<7521>
2. [x] Add the initial product domain. %<7522>
   1. [x] Create client and invoice models. %<7523>
   2. [x] Create the initial client and invoice migrations. %<7524>
   3. [x] Add `user_id` ownership columns to both resources. %<7525>
   4. [x] Wire model relationships back to the authenticated user. %<7526>

## 2. Wire the per-user resource layer

1. [x] Add client and invoice controllers. %<7528>
2. [x] Add authenticated routes for the initial resource actions. %<7529>
3. [x] Scope resource creation to the authenticated user's ID. %<7530>
4. [x] Add the first per-user defaults/address support used by the pre-MS5 invoice model. %<7531>
5. [x] Add the `/health` route and clean up redundant controller middleware during the early foundation pass. %<7532>

## 3. Ship client management

1. [x] Add client list, create, and edit views. %<7534>
2. [x] Add client validation and persistence. %<7535>
3. [x] Add soft deletion to clients. %<7536>
4. [x] Add the client trash listing. %<7537>
5. [x] Add restore and permanent-delete actions. %<7538>
6. [x] Link client management from authenticated navigation. %<7539>

## 4. Verify and hand off

1. [x] Merge per-user defaults/routes through PR #1. %<7541>
2. [x] Merge BTC-default/route cleanup through PR #2. %<7542>
3. [x] Merge client UI and soft-delete behavior through PR #3. %<7543>
4. [x] Leave models, routes, and client selection ready for the invoice lifecycle phase. %<7544>

## Phase Exit Criteria

- [x] The Sail application boots with authentication and persistent client/invoice tables. %<7546>
- [x] Clients and invoices are associated with users. %<7547>
- [x] Authenticated users can create, edit, soft-delete, restore, and permanently delete clients. %<7548>
- [x] The client surface is ready to supply invoice recipients in Phase 2.2. %<7549>

## Historical boundary

This phase established per-user data ownership structurally, but the explicit Laravel policy layer and friendly shared `403` response landed later in MS1.
