# M21 Phase 3 — Verify the Public Result and Sign Off

Status: Approved by n8 - 9/5/2026 21:28.
Parent: [M21](../milestones/21_OB_DEPLOYMENT.md)
Prerequisite: [M21.2](x21.2_DEPLOY_AND_CUTOVER.md) complete.

Goal: verify the changed public entry points and accept the in-place transition. M20's wallet/payment/provider proofs remain the [accepted baseline](../milestones/21_OB_DEPLOYMENT.md#accepted-baseline).

Parallel work: public URL checks (§2) and service observation (§3) can run alongside the account smoke (§1).

## 1. [ ] Verify public access and links

1. [x] Register an operator-controlled account and confirm it can sign in and reach wallet setup without manual alpha approval. `invoicer+m21beta@cybercreek.us` (user 4) registered from a fresh browser context, landed signed in on the getting-started welcome, and opened `/wallet/settings`; no pending-approval step, no verification mail. Credentials in `.cybercreek/ms21-phase3/`.
2. [x] Using the existing verified operator account, create an invoice and open its enabled share link signed out. INV-0002 (id 4) created on the apex; public link enabled and opened from a fresh browser context.
   1. [x] Verify the apex URL, displayed invoice/QR, and noindex; do not onboard another copy of the operator wallet or send another payment. `https://cryptozing.app/p/{token}` 200 with `X-Robots-Tag: noindex, nofollow, noarchive` and the matching meta; invoice, address, and QR rendered; no analytics script. Link disabled after the check.
3. [ ] [User] Check Phase 2's received invoice email and open its apex link. Reuse that delivery; no second send is needed.
4. [x] Confirm old alpha URLs behave according to the approved retirement disposition. `alpha` has no DNS at Namecheap or Cloudflare; https/http straight to the box are dropped (444); apex, `www`, and http redirects unaffected.

## 2. [x] Verify public-site continuity

1. [x] Compare the published URL inventory with Phase 1's baseline on the new serving path. 9 sitemap URLs, 7 staging pages, 14 support files, slash and `www`/http redirects all match. Differences: `/index.html` 404 (Pages artifact, unlinked, not in the sitemap; canonical is `/`); `/site.webmanifest` typed `application/octet-stream` ([cryptozing-site#4](https://github.com/n8bar/cryptozing-site/issues/4)); unknown paths get Laravel's 404.
   1. [x] Resolve missing articles/assets, changed redirects, or broken sitemap/verification files. Nothing missing; redirects unchanged (301, one hop, absolute https); sitemap root `lastmod` 2026-09-06, robots `Allow: /` + sitemap, IndexNow key file serves the key. The manifest type rides §3.2.
   2. [x] Verify public canonicals/indexing and retained staging/invoice noindex. Landing and articles canonical to the apex; staging `noindex, nofollow` with canonical to `/learn/`; public invoice views `noindex, nofollow, noarchive` in header and meta; no `X-Robots-Tag` elsewhere.
2. [x] Verify the landing page has its intended metadata, guides navigation, and working policy links; policies have the publication date and agreed analytics wording. Title, description, canonical, og/twitter, JSON-LD, `og-preview.png`; Guides → `/learn/`; footer Terms/Privacy 200. Both policies "Effective: September 6, 2026"; Privacy §2 carries the approved first-party wording and §7 names no analytics recipient.
3. [x] Verify the selected analytics endpoint receives landing/article views without duplicate collection or events from excluded surfaces. Fresh-context views of `/` and an article each produced one `cryptozing.app` pageview in the self-hosted store; `/login` produced none; only the self-hosted script loads (no Cloud tag); staging and public invoice views carry no script.

## 3. [ ] Confirm operation and accept the release

1. [x] Check [service health and watcher progress](../ops/RUNNING_THE_SERVER.md#reading-health-honestly) after the final restarts; inspect logs/queue failures for cutover errors. app/queue/scheduler/db/umami healthy, site and nginx up, 0 zombies, no failed jobs; app log clean since 2026-08-29; nginx errors are blocked probes only. Watcher was parked: a `framework/schedule-*` mutex from 00:23 local (recreate mid-run) survived; deleted per the runbook at 15:04 local and the stamp advanced on the next tick ([#188](https://github.com/n8bar/CryptoZing/issues/188)). Pre-cutover log errors filed as [#187](https://github.com/n8bar/CryptoZing/issues/187).
2. [ ] Resolve cutover findings and repeat only their affected checks; present any proposed deferrals for acceptance.
3. [x] Correct deployment/runbook references changed by alpha retirement; update AGENTS.md's data-hygiene note if real customer data now exists. Tracked docs carry no alpha serving references; the overlay keeps its `compose.alpha.yaml` name and template default per the milestone scope; `deploy.sh`'s comment and the runbook's recreate note ride [#189](https://github.com/n8bar/CryptoZing/pull/189). Data-hygiene note kept: prod holds operator accounts, one alpha-era sign-up test row, and the §1.1 smoke account.
4. [ ] [User] Accept the public deployment after reviewing the cutover and verification verdicts.
5. [ ] Complete the [milestone's content-promises check](../milestones/21_OB_DEPLOYMENT.md#exit-criteria), close M21, and hand discovery observations and accepted follow-ups to [M22](../milestones/22_SEO_MARKETING.md).

## Exit Criteria

- [ ] Public access, hostname retirement, mail links, and content/publication checks pass.
- [ ] Services are healthy and the watcher advances, with no unresolved cutover blocker; the operator accepts the result and M21's closeout is complete.
