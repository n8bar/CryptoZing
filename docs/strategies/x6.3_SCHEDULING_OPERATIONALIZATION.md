# MS6 Phase 3 Strategy - Scheduling & Operationalization

Status: Complete (retrospective reconstruction).
Parent milestone: [`docs/milestones/x6_BLOCKCHAIN_PAYMENT_DETECTION.md`](../milestones/x6_BLOCKCHAIN_PAYMENT_DETECTION.md)
Historical execution window: 2025-11-10 through 2025-11-11.

> This phase boundary was reconstructed on 2026-07-18 around command registration and scheduler integration. It was not named or checked off as Phase 6.3 during original execution.

## Goal

Make the watcher discoverable by Artisan and keep it executing every minute without overlapping runs.

## Reconstruction basis

Primary evidence: command-registration commit `4c75c31` and scheduling/test commit `a961df0`, plus the corresponding 2025-11-10 changelog entries.

## 1. Register the command

1. [x] Register application commands through `bootstrap/app.php`. %<7874>
2. [x] Make `wallet:watch-payments` available to Artisan. %<7875>
3. [x] Preserve the optional single-invoice invocation for targeted checks. %<7876>

## 2. Add the Laravel schedule

1. [x] Configure `wallet:watch-payments` to run every minute. %<7878>
2. [x] Add `withoutOverlapping()` so a slow run cannot stack with the next minute. %<7879>
3. [x] Add `runInBackground()` so the schedule runner is not blocked by the watcher process. %<7880>

## 3. Add schedule regression coverage

1. [x] Resolve Laravel's configured schedule in a feature test. %<7882>
2. [x] Find the event whose command contains `wallet:watch-payments`. %<7883>
3. [x] Assert the event exists. %<7884>
4. [x] Assert its cron expression is every minute (`* * * * *`). %<7885>
5. [x] Assert overlap prevention is enabled. %<7886>
6. [x] Assert background execution is enabled. %<7887>

## 4. Close the milestone

1. [x] Update PLAN with the watcher and scheduling outcome. %<7889>
2. [x] Record command wiring and automated scheduling in the changelog. %<7890>
3. [x] Leave manual `wallet:watch-payments` available for targeted operational checks. %<7891>
4. [x] Hand continuous watcher execution to the running Laravel scheduler process. %<7892>

## Phase Exit Criteria

- [x] Artisan recognizes `wallet:watch-payments`. %<7894>
- [x] Laravel schedules it every minute. %<7895>
- [x] Scheduled runs cannot overlap and execute in the background. %<7896>
- [x] A feature test locks the schedule configuration. %<7897>

## Historical boundary

This phase registered the Laravel schedule; it did not yet add the dedicated Compose `scheduler` service that continuously runs `php artisan schedule:work`. That container-level operational support landed later and should not be attributed to MS6.
