# MS20 Phase 3 Strategy — Taproot Wallet Support

Status: Scope approved — implementation sections flesh out after the §1 spec delta is approved.
Parent milestone doc: [`docs/milestones/20_MAINNET_CUTOVER_ALPHA_GATE.md`](../milestones/20_MAINNET_CUTOVER_ALPHA_GATE.md)
Supporting spec: [`docs/specs/WALLET_XPUB_UX_SPEC.md`](../specs/WALLET_XPUB_UX_SPEC.md)

**Goal:** The watch-only wallet layer accepts Taproot (BIP86) account keys and derives bech32m (`bc1p`/`tb1p`) receive addresses for invoices and donations, so the Phase 4 operator wallets — and merchants at cutover — can be Taproot. Watch-only invariant unchanged: public keys in, addresses out, nothing that can sign.

## Decisions to confirm before flesh-out

1. Protocol coverage: Taproot (BIP86) only, or also other onboarding gaps surfaced by the same review (wrapped SegWit/BIP49, descriptor input, multisig)? _Answer:_ Taproot + descriptor input (`wpkh(...)`/`tr(...)`). Named exclusions, recorded in the spec: wrapped SegWit/BIP49, Silent Payments (BIP352 — detection needs a scan key, doesn't fit address-polling watch-only), Lightning (separate layer; sequenced after Ethereum support, which itself is deliberately not backlogged). Multisig stays in [`docs/BACKLOG.md`](../BACKLOG.md) §3.

## 1. [ ] Spec first

1. [ ] Extend `WALLET_XPUB_UX_SPEC.md`: supported script types, the bare-`xpub` ambiguity (same key string is a valid BIP84 or BIP86 account key, so onboarding must capture script type), and error/guidance copy.
2. [ ] [User] Approve the spec delta before implementation starts.

## 2. [ ] Implementation

_Fleshed out after §1 approval. Expected shape: script-type-aware onboarding at `/wallet/settings`, per-account script type in the schema, BIP86/`p2tr` derivation in `node_scripts/derive-address.cjs`, unsupported-config detector and key-lineage updates, tests. Rides `claude/taproot-wallet-support` + PR._

## 3. [ ] Verification

_Fleshed out with §2. Must prove: derived `tb1p`/`bc1p` addresses match the source wallet at m/86'; watcher detects payments to a Taproot address on Testnet4; BIP84 onboarding unchanged._

## Exit Criteria

- [ ] A Taproot account key onboards at `/wallet/settings` and derives correct bech32m addresses, verified against the source wallet.
- [ ] Existing BIP84 (`zpub`/`vpub`/bare `xpub`) onboarding behavior is unchanged.
- [ ] Watch-only audit clean: no signing material anywhere in the new paths.
