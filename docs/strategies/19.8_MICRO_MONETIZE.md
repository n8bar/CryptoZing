# MS19 Phase 8 Strategy — Micro-Monetize

Status: Fleshed out — pending review; not yet approved implementation direction.
Parent milestone doc: [`docs/milestones/19_OB_HARDENING_OPS.md`](../milestones/19_OB_HARDENING_OPS.md)
Spec: [`docs/specs/DONATIONS.md`](../specs/DONATIONS.md)

**Goal:** Stand up CryptoZing's first revenue surface — a donation page — as a low-stakes entry into monetization. Makes the "leave room for monetization" posture concrete without committing to fees or paid tiers.

## Scope
**BTC-only.** Donations route to a CryptoZing wallet address; no bank account involved. The phase stays in MS19. Fiat donations are out of scope — backlogged as [`docs/BACKLOG.md`](../BACKLOG.md) item 25.

## Decisions made
- **Surface:** a public, guest-safe `/donate` route in the Laravel app. Ships dark; goes public with the MS21 deploy.
- **Addresses:** derived per donor session from a CZ-owned donation xpub (watch-only), reused until paid. Derive only on explicit donor action — never on page load — with a capped unpaid-address pool as the bot/gap-limit guard.
- **Amounts:** USD presets + custom field, converted at the live rate; chosen amount prefilled in the QR (BIP21).
- **After-pay:** on-page thank-you state when payment is seen. Anonymous — no personal data collected.
- **Architecture:** separate donation model (small donations table + address allocation) reusing the existing derivation and payment-watching services; the invoice domain stays untouched.

## 1. Donation wallet & addresses
1. [ ] Add donation-wallet config: a CZ-owned donation xpub, separate from all user wallets; follows the app's network config (testnet now, mainnet at cutover).
2. [ ] Flag the donation xpub in the MS20 cutover checklist (env flip + wallet validation) so cutover swaps it with everything else.
3. [ ] Migration: donations table + donation-address allocation state.
4. [ ] Allocation service: derive on explicit donor action; address reused until paid; capped unpaid pool (bot/gap-limit guard).
5. [ ] Extend the payment watcher to cover allocated donation addresses; record seen payments as donations (never invoices).

## 2. Donation page
1. [ ] Public, guest-safe `/donate` route + page: USD presets + custom field, live-rate BTC conversion, derived address with copy button and BIP21 QR.
2. [ ] Thank-you state when the page's address sees payment.
3. [ ] Visual pass with the `frontend-design` plugin; UX guardrails apply ([`docs/UX_GUARDRAILS.md`](../UX_GUARDRAILS.md)).

## 3. Legal / disclaimer copy
1. [ ] On-page disclaimer: donations support CryptoZing LLC, non-refundable, not tax-deductible — wording per Phase 5 conventions and the monetization-language guide.
2. [ ] Content promises check: donation-page copy introduces no new promises and violates none (0% beta fee stays true — donations are not fees).

## 4. Tests
1. [ ] Feature: `/donate` renders guest-safe with presets and a derived address after donor action.
2. [ ] Feature: allocation — fresh address per session, reuse until paid, pool cap enforced.
3. [ ] Feature: watcher attributes donation-address payments to donations, not invoices.
4. [ ] Feature: thank-you state on payment seen.
5. [ ] Feature: donations absent from invoice surfaces/stats.

## 5. QA
1. [ ] [User] Browser QA: full donor flow on testnet — preset → QR/copy → send → thank-you.
2. [ ] [Agent] Verify gap-limit guard behavior under repeated anonymous sessions.

## Exit Criteria
1. [ ] `/donate` ships in the app build MS21 deploys (dark until then); donor flow works end to end on testnet.
2. [ ] Donations recorded in their own model; zero leakage into invoice surfaces, stats, or mail.
3. [ ] Disclaimer copy present; monetization-language guide satisfied; content promises reconciled for the new page.
4. [ ] Tests in §4 green; QA in §5 signed off.

## Dependencies
- **Phase 5 (Legal Layer)** — complete; donation disclaimer follows its conventions.
- **Entity formation (Phase 3)** — complete.

## Reference
- Business-ops follow-ups (banking, insurance, tax handling) are tracked outside the repo.
