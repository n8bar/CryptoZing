# MS19 Phase 5 Strategy — Legal Layer

Status: Complete — §1–§7 done, eyes-on verification included. Publication (effective dates + live links) lands at MS21 deploy.
Parent milestone doc: [`docs/milestones/x19_OB_HARDENING_OPS.md`](../milestones/x19_OB_HARDENING_OPS.md)

**Goal:** Draft ToS, Privacy Policy, and disclaimer copy; review existing UI/mail copy for monetization-neutral language; scaffold UI placement so the legal layer is staged for activation. Drafting and scaffolding land in this phase. The entity name is final (CryptoZing LLC, formed in Phase 3); publication to the live site — resolving the remaining effective-date and Privacy Policy link placeholders — is the deploy-time finishing step in MS21, *not* in this phase.

Note on phase ordering: Phase 3 (LLC) and Phase 5 (Legal Layer) run as independent parallel tracks; neither gates the other phase-by-phase. Both must land before MS21.

Decisions inherited from the milestone doc that flow into this phase:
- **Legal approach:** No lawyer for RC1. Self-drafted ToS and Privacy Policy covering essentials.
- **Disclaimer surfaces:** Account signup, wallet onboarding, invoice/payment screens. Footer links to ToS and Privacy Policy on every page.
- **Monetization-neutral language:** Avoid language that forecloses pricing options.

## Decisions made

- **Disclaimer wording approach:** Context-specific copy at each surface (signup vs. wallet onboarding vs. invoice/payment). Small print, ultra-concise without sacrificing necessary detail.
- **Pages location:** Both — `/terms` and `/privacy` on the public GitHub Pages site (`site/`) AND in the Laravel app. App needs them reachable from authenticated views; the public site needs them for unauthenticated visitors and SEO.
- **Monetization-safe language guide location:** Section in [`docs/UX_GUARDRAILS.md`](../UX_GUARDRAILS.md). Fits the existing copy-rules section; consulted during copy work, which is exactly when this guide matters.
- **Existing-copy review scope:** Sweep all UI and mail copy — not limited to public-facing surfaces.
- **"Open beta" copy refactor placement:** Moved to Phase 4 (Visual Identity Polish) — bundled with favicon overhaul and og:image work as one coherent visual/brand pass.

## 1. [x] Draft Terms of Service %<9820>

_Status:_ draft complete at [`../legal/CryptoZing_ToS_DRAFT.md`](../legal/CryptoZing_ToS_DRAFT.md) — section review and disposition-walk edits approved. Post-beta framing removal tracked in [#120](https://github.com/n8bar/CryptoZing/issues/120).

1. [x] Adapt the ToS skeleton + risk boilerplate from the **Basecamp/37signals policies** base (CC BY 4.0): limitation of liability, AS IS / AS AVAILABLE warranty disclaimer, indemnification, governing law (Arizona), dispute terms, termination, acceptable use, IP, severability. %<9821>
2. [x] Lead with a preamble disclosing operational reality: solo operator, first fintech, recommended-use language ("for low-stakes invoices first; not for production-critical workflows"). %<9822>
3. [x] Draft the product-specific clauses from scratch — no template covers these: not financial advice, no custody of funds, user responsibility for keys/seed, watch-only wallet model, no warranty on BTC/USD values, exclusion of consequential damages including third-party amounts. %<9823>
4. [x] Assemble the full ToS with entity name `[CryptoZing LLC]`; include the CC BY 4.0 attribution line the base requires. %<9824>
5. [x] Finalize §9 (Stopping & termination) open threads: %<9825>
   1. [x] Scope wording for what account deletion removes. Decided full deletion — profile, wallet configuration, invoices, payment records; shared invoice links stop working. Wording applied to §9; drives the [#121](https://github.com/n8bar/CryptoZing/issues/121) fix (item 7) and the PP retention section. %<9826>
   2. [x] Survival clause: omit. Neither the Basecamp base nor the WP.com ToS in Legalmattic carries one; proposed line removed from §9. %<9827>
   3. [x] Add one general Privacy-Policy pointer elsewhere in the ToS. Placed in §15: the entire-agreement sentence now names the `[Privacy Policy]`, plus a one-line pointer (what we collect / how we handle it). %<9828>
6. [x] Self-review pass: cross-check boilerplate completeness against Automattic Legalmattic; cross-check the non-custody/keys phrasing against BTCPay Server's terms for gaps. Both cross-checks run; 15 findings — walked via item 7, dispositions and resulting edits tracked in item 9. %<9829>
7. [x] Walk thru the self-review findings and populate 1.9 with the actions to take. Declined: blockchain-irreversibility sentence (closest comparable omits it; §2/§5 no-reversal acknowledgements suffice); DMCA hook (invoice-text content surface too thin; §6/§9/§16 already cover removal and contact); fees seam (ToS stays fully silent on pricing — silence forecloses nothing, §14 carries any future fee introduction, and "currently free" wording would rot). %<9830>
8. [x] Fix [#121](https://github.com/n8bar/CryptoZing/issues/121) — account self-deletion FK violation — so deletion delivers §9's promise: payment/accounting records delete with the account (full deletion per item 6.1); clean up orphaned session rows while in there. Landed via PR #122 (deleting hook + transaction; sessions purge + cascade FK); suite green, dev DB migrated. %<9831>
9. [x] Edit pass from the item-6 self-review disposition walk — all 12 edits applied and diff-approved. %<9832>
   1. [x] Sanctions/eligibility clause: add — generic regime reference only (administering body, e.g. OFAC-administered lists), no country names, nothing that needs updating as world politics shift. %<9833>
   2. [x] Payer coverage: add — CryptoZing is not a party to the merchant↔payer transaction; scope what a payer can rely on from the public invoice page. %<9834>
   3. [x] Authority-to-bind rep (§1): add — signing up for an organization binds the organization; "you" includes it. %<9835>
   4. [x] Feedback license: add — suggestions are free for CryptoZing to use, no obligation or compensation. %<9836>
   5. [x] xpub visibility (§2): add acknowledgement bullet — CryptoZing itself sees the connected account's receive history and balance, not just invoice payments; PP carries the detail. %<9837>
   6. [x] §3 verification duty: extend to payment statuses (alongside addresses and amounts). %<9838>
   7. [x] Compromised accounts: add — user duty to promptly notify us of unauthorized access, and our right to stop serving a compromised account. Scope tightly to service access — no language implying we can protect, hold, freeze, or recover funds. %<9839>
   8. [x] Tax responsibility (§4): add, minimal — one sentence allocating tax determination/payment to the user; displayed rates and amounts aren't tax records. %<9840>
   9. [x] Force majeure (§11): add, very minimal — one sentence, no event laundry list. %<9841>
   10. [x] §11 any-legal-theory catch-all: add phrase to the cap sentence — contract, tort (including negligence), or any other theory. %<9842>
   11. [x] §11/§12 protected parties: widen — cover the ownership chain (member entities and their owners, not just "its owner") plus employees, contractors, and agents; no individual named by name. %<9843>
   12. [x] §15 supplemental-policy openness: add — future policies join the agreement only when introduced with §14 notice; no silent-incorporation door. %<9844>

## 2. [x] Draft Privacy Policy %<9845>

_Status:_ complete — draft at [`../legal/CryptoZing_PP_DRAFT.md`](../legal/CryptoZing_PP_DRAFT.md), all sections approved.

1. [x] Adapt the PP structure from the **Automattic Legalmattic** privacy base (CC BY-SA 4.0): data collected, mail handling, third-party processors (Mailgun for outbound mail, etc.), retention, no third-party tracking commitments, user rights. Kept: collect (provided/automatic), use, sharing, retention, security, choices, changes, contact. Dropped: GDPR legal bases, US-state rights machinery, ads/personalization, controllers table, other-sources collection (no social login). %<9846>
2. [x] Draft the full PP with entity name CryptoZing LLC; carry the CC BY-SA attribution + same-license note the base's ShareAlike term requires. %<9847>
3. [x] Include the xpub-visibility disclosure: what the xpub lets us see (account receive history and balance), what we use it for (payment detection), what we don't do with it, deletion with the account per ToS §9. PP §3; also covers that watching sends only derived addresses, never the xpub, to the blockchain data service. %<9848>
4. [x] Self-review pass. Fact-checked against a code data-flow inventory (models, mail, external APIs, logging, deletion cascade) and the Legalmattic base section list; one fix — dropped a "short-lived" log-retention overpromise in §8. %<9849>
5. [x] [Guided] Have the agent output one section at a time with a very concise explanation of its purpose, effectiveness, and how bad we need it. All sections approved. Notables: §3 purpose-bound xpub use ("anyone holding the xpub — including CryptoZing — can see"); §7 vendors illustrative ("like Mailgun") with an as-we-grow bullet; §9 support conditional ("If support is provided"); §10 names the self-serve design goal; §12 contact CryptoZingPrivacy@CyberCreek.us; CC BY-SA footer retained (MIT swap not license-permitted). %<9850>

_ToS §9 alignment:_ the PP's deletion/retention section mirrors ToS §9: full deletion on account deletion (profile, wallet configuration, invoices, payment records), with carve-outs only for data other users hold and records CryptoZing keeps as a transaction party or must retain. Implementation fix tracked in [#121](https://github.com/n8bar/CryptoZing/issues/121) (§1 item 8).

## 3. [x] Draft disclaimer copy %<9851>

_Status:_ complete — all three disclaimers approved at [`../legal/CryptoZing_DISCLAIMERS_DRAFT.md`](../legal/CryptoZing_DISCLAIMERS_DRAFT.md).

1. [x] Write context-specific disclaimer for signup (account creation context). Beta/low-stakes framing + clickwrap line, below the Register button. %<9852>
2. [x] Write context-specific disclaimer for wallet onboarding (key responsibility context); include the first-person visibility fact — current copy only warns about leak risk, not that CryptoZing sees the account. Single sentence appended to the wallet page's amber privacy note; visibility-only (PP §3 wording). %<9853>
3. [x] Write context-specific disclaimer for invoice/payment (rate/payment timing context). Two sentences on the public invoice page (screen + print): figures informational/not guaranteed, verify before relying, not a party to the transaction — carries the payer-facing limits per §6.2.2. %<9854>
4. [x] Each: small print, ultra-concise, entity-agnostic where possible. All three meet it — service name only, no LLC. %<9855>

## 4. [x] Monetization-neutral copy review %<9856>

_Status:_ complete — UI and mail clean; article flags declined (published articles are not revised).

1. [x] Sweep all UI strings — Blade templates, components, error messages, button labels, microcopy. App UI, JS, and help/welcome surfaces clean; flags confined to `site/` articles. %<9857>
2. [x] Sweep all mail copy — Mail classes and their templates. All 12 templates + vendor components + subjects clean. %<9858>
3. [x] Flag language that forecloses pricing options (e.g., "always free," "no fees ever"). Three flags, all in published `site/` articles — declined; published articles are not revised. Mail tagline "Non-custodial bitcoin invoicing" dismissed — custody isn't a pricing promise. %<9859>
4. [x] Revise flagged items in place. Nothing to revise — no accepted flags. %<9860>

## 5. [x] Monetization-safe language guide %<9861>

_Status:_ complete.

1. [x] Add new section to [`docs/UX_GUARDRAILS.md`](../UX_GUARDRAILS.md): "Monetization-neutral language." %<9862>
2. [x] Include concrete rules (don't promise no fees / always free; do leave room for future paid tiers) and 3–5 do/don't examples. Four rules (time-bound claims, architecture-not-pricing, present-tense facts, competitor-criticism aim) + four do/don't pairs. %<9863>
3. [x] Reference from `CONTENT_PROMISES.md` when adding new entries that touch pricing language. One line at the end of "When to add an entry." %<9864>

## 6. [x] UI placement %<9865>

1. [x] Add disclaimer copy at signup, wallet onboarding, invoice/payment surfaces. All three verbatim from the approved drafts. %<9866>
   1. [x] Signup: the ToS and Privacy Policy links in the signup disclaimer ([`../legal/CryptoZing_DISCLAIMERS_DRAFT.md`](../legal/CryptoZing_DISCLAIMERS_DRAFT.md) §1) must open the readable documents at the point of agreement, so the policies can actually be read before they bind. In-app `/terms` and `/privacy` render the approved drafts from `resources/markdown/`; cross-document references are live links, only `[date]` remains for MS21. %<9867>
2. [x] Add footer ToS / Privacy Policy link scaffolding (placeholder URLs to `/terms` and `/privacy`; final URLs activate at MS21 deploy). Scope: %<9868>
   1. [x] Authenticated app pages and public site pages: both links. App, guest, and public layouts via `x-legal-footer`; `site/` layout footer + homepage. %<9869>
   2. [x] Public invoice pages: Privacy Policy link only — payer-facing limits are carried on-page by the invoice/payment disclaimer (§3.3), not by a ToS the payer isn't a party to. %<9870>
   3. [x] Print views: no footer, no links. Footer is `no-print`; the §3.3 disclaimer intentionally prints. %<9871>
3. [x] Verify remaining placeholders (effective date, Privacy Policy links) are consistent throughout for the MS21 publish step. Subagent-verified: rendered copies word-identical to approved drafts; the two `[date]` literals are the only remaining user-facing placeholders. %<9872>
4. [x] Verify our drafted items in this stage that use open source licensed texts meet attribution and any other requirements of the respective licenses. Verified against CC BY 4.0 / CC BY-SA 4.0 legalcode; two gaps fixed by hyperlinking source + license texts in both attributions (no wording changes). %<9873>
5. [x] [User] Eyes-on verification of the user-facing copy at each placement: %<9874>
   1. [x] [User] Signup disclaimer on the registration page. Verified with wording tweak (em-dash split; "Suggested use is for… for now"; "critical" un-prefixed) — draft §1 + view in sync. Footer-contrast finding (#128) fixed and approved. %<9875>
   2. [x] [User] Wallet disclaimer appended to the amber privacy note on wallet settings. Verified; adjacent amber-note copy tweaked in passing ("screenshots or pastes that include it", `8b6b37d`). %<9876>
   3. [x] [User] Invoice/payment disclaimer on the public invoice page — screen and a printed/print-preview copy. Verified, both renderings. %<9877>
   4. [x] [User] `/terms` and `/privacy` pages render the approved drafts. Verified. %<9878>
   5. [x] [User] Footer links: app and site pages show both, public invoice page shows Privacy Policy only, print shows none. Verified, all four surfaces; guest-layout contrast fixed via #128. %<9879>

## 7. [x] Open-issue sweep %<9880>

Every open GitHub issue gets a disposition: fixed in this phase, or assigned an upcoming todo home.

1. [x] [#128](https://github.com/n8bar/CryptoZing/issues/128) Footer legal links nearly invisible on dark-mode pages — fixed this phase (guest-layout footer vars, `0f607db`); closes on merge. %<9881>
2. [x] [#71](https://github.com/n8bar/CryptoZing/issues/71) New Invoice: inline "New client" option in Client dropdown. _Disposition:_ fix in this phase. %<9882>
   1. [x] Decide disclosure mechanism: inline expanding panel above the dropdown (issue lean) vs. modal. _Answer:_ modal — reuses the existing `<x-modal>` component and meets the no-displacement criterion by construction. %<9883>
   2. [x] Backend: reuse `clients.store` for the XHR path — JSON responses for created client and validation errors. Already supported (`wantsJson()` 201 + automatic 422); no code change, pinned by tests. %<9884>
   3. [x] UI: `+ New client` option pinned at the top of the Client dropdown, separator below it. %<9885>
      1. [x] Selecting it opens the Name + Email prompt without navigating; all invoice-form input preserved. %<9886>
      2. [x] Submit via XHR; on success, dropdown re-renders with the new client pre-selected (alphabetical insert) and the prompt closes. %<9887>
      3. [x] Validation errors render inline in the prompt — no reload, input preserved. %<9888>
   4. [x] Focus & a11y: focus enters the Name field on open, returns to the dropdown on close with the new client selected; keyboard-reachable; mode change announced to screen readers. Shared `<x-modal>` now emits `modal-closed`, so focus-return covers cancel/Escape/overlay too; prompt is `role="dialog"` + `aria-modal` + labelled. %<9889>
   5. [x] Decide: collapse the `$showClientGate` empty-state form into this flow, or keep the gate as the no-clients path. _Answer:_ keep the gate — preserves the first-run guidance and its getting-started onboarding wiring; the two surfaces share `clients.store` and the form-fields partial. %<9890>
   6. [x] Tests: feature coverage for the JSON create path and validation errors; run the full suite. `InvoiceCreateNewClientTest` (JSON 201/422/401, dropdown+prompt render, gate intact); full suite 391/391. %<9891>
   7. [x] [User] Eyes-on — desktop + mobile, both themes: passed. %<9892>
      1. [x] Preserved input: invoice-form fields stay populated through the whole create-client flow. %<9893>
      2. [x] No layout shift: the prompt discloses without displacing the rest of the form. %<9894>
      3. [x] Inline errors: Name/Email validation renders in the prompt; no full-page reload. %<9895>
      4. [x] Focus: enters the Name field on open; returns to the dropdown on close with the new client selected. %<9896>
      5. [x] Mobile/a11y: keyboard-reachable end to end; screen reader announces the prompt opening. %<9897>
   8. [x] Close #71 via the implementing PR. PR #130 merged. %<9898>
3. [x] [#72](https://github.com/n8bar/CryptoZing/issues/72) Underpayment warnings fire on invoices with no payment activity. _Disposition:_ fix in this phase by completing and landing external [PR #88](https://github.com/n8bar/CryptoZing/pull/88). Landed (merge `856ba4d`); #72 closed. %<9899>
   1. [x] Review PR #88's short-circuit: no confirmed *and* no pending payments ⇒ not underpaid; outstanding-balance summary math unchanged; unit coverage for fresh-unpaid and real-partial-payment cases. Verified: short-circuit fires only on zero non-ignored payments; `hasPaymentActivity()` symmetric with `activePayments()`; fresh-invoice test pins outstanding at 100%; 0 conflicts vs current `main` (incl. both paid_at commits); merged-state suite 5/5. Observation, in-scope-per-issue: a pending-unconfirmed payment still warns until confirmation (pre-existing behavior). %<9900>
   2. [x] Audit every call site of `hasSignificantUnderpayment()` / `underpaymentPercent()` / `requiresClientUnderpayAlert()` for consistent awaiting-payment behavior (the PR touches only the model): %<9901>
      1. [x] Issuer view `invoices/show.blade.php` (both warning strings). Gated directly on the patched methods — inherits the fix. %<9902>
      2. [x] Public/print view `invoices/partials/print/content.blade.php` (outstanding-balance string). Same — inherits. %<9903>
      3. [x] Mail templates and notification surfaces. No template renders underpay copy directly; `InvoiceAlertService` and `DeliverInvoiceMail:383` both gate on `requiresClientUnderpayAlert()` — inherit the fix. %<9904>
   3. [x] Fill review gaps — at minimum, feature-level coverage that a fresh invoice renders none of the three warning strings and a genuinely underpaid one still warns. Added `test_fresh_invoice_without_payments_shows_no_underpayment_warnings` (issuer + print views); underpaid-still-warns already covered at feature and unit level. Full suite 386/386. %<9905>
   4. [x] [User] Eyes-on with a fresh unpaid invoice: issuer view, public view, and print all read as awaiting payment; a partially paid invoice still shows the underpayment warnings. Verified via seeded fixtures (INV-EYES-FRESH / INV-EYES-PARTIAL): fresh renders neutral Sent + outstanding balance, no warning banners; partial shows the 40% warnings. ("Awaiting payment" = neutral unpaid state, not a literal string.) %<9906>
4. [x] [#73](https://github.com/n8bar/CryptoZing/issues/73) Client-facing "contact {biller}" copy — review framing. _Disposition:_ review in this phase — likely already addressed by later copy work; expands into fix items here if the review says otherwise. Closed as addressed. %<9907>
   1. [x] [Agent] Compare current copy at the four `print/content.blade.php` spots (overpayment + gratuity note, overpayment fallback, underpayment/outstanding, gratuity-only) against the issue's three concerns: implied support relationship, biller-name brand leakage, no actionable next step. Three spots reworked to concrete-action framing; residue = the outstanding-balance "for assistance" clause. %<9908>
   2. [x] [User] Verdict on the findings: close #73 as addressed, or expand this item with the fix work. Clause kept deliberately — post-#72 the line renders only on genuine partials, where a human fallback is warranted; verified against the rendered fixture. Closed #73. %<9909>
5. [x] [#81](https://github.com/n8bar/CryptoZing/issues/81) Re-run stress testing when Mailgun upgraded/switched. _Disposition:_ todo home added in [`x21_OB_DEPLOYMENT.md`](../milestones/x21_OB_DEPLOYMENT.md) carried items — likely executes post-M21 at mailer upgrade/switch; issue stays open as the tracker. %<9910>
6. [x] [#120](https://github.com/n8bar/CryptoZing/issues/120) Post-beta: drop beta framing from ToS/PP. _Disposition:_ todo home added in [`BACKLOG.md`](../BACKLOG.md) "Legal & Policy" (item 21), preamble harmonization folded in; issue stays open as the tracker. %<9911>
7. [x] [#123](https://github.com/n8bar/CryptoZing/issues/123) Document the wallet gap-limit pitfall. _Disposition:_ fix in this phase — new Helpful Notes (`/help`) entry; wallet-settings pointer decided during review. Closes on merge. %<9912>
   1. [x] [Agent] Draft the note: why an invoice can read paid while the wallet shows nothing (gap limit, plain terms); funds intact and seed-controlled; resolution — raise the wallet's gap limit and/or rescan per its docs. %<9913>
   2. [x] [User] Review and approve the copy. Final wording merged from Nate's rewrite (worked 25-invoice example, plain register) + gap-limit named in the intro + forum/support escalation line. %<9914>
   3. [x] Place the approved note on `/help` alongside the existing wallet-safety entries; update the page meta description if the topic list changes. Placed as `#gap-limit` article under Payments, dt/dd Q&A idiom; meta description still accurate, unchanged. %<9915>
   4. [x] Decide: one-line pointer from wallet settings (near the xpub field) to the note — include or skip, wording at review. _Answer:_ skip for now — no balance surface exists to anchor it. Wallet-balance display backlogged ([`BACKLOG.md`](../BACKLOG.md) item 22); the pointer decision rides that implementation. %<9916>
   5. [x] [User] Eyes-on: note renders on `/help` in both themes; wallet-settings pointer (if included) reads right and links correctly. Verified post-tweaks; no pointer per §7.7.4. %<9917>
8. [x] [#126](https://github.com/n8bar/CryptoZing/issues/126) APP_NAME still "Laravel"; leaks into help meta + mail sign-off fallback. _Disposition:_ fix in this phase, plus a project-wide placeholder-branding sweep. Closes on merge (`32701eb`). %<9918>
   1. [x] Set the app name to CryptoZing across the env layers (`.env`, `.env.example`, `config/app.php` fallback); confirm the two known leaks render correctly (help meta description, client-mail sign-off). Done; the help leak was the og:title (description was already clean), and the sign-off fallback exists in three mail templates — all resolve via `config('app.name')`, verified rendering CryptoZing. %<9919>
   2. [x] [Subagent] Sweep all user-facing surfaces for placeholder branding: blade views and mail templates (rendered strings, from-names, subjects), `site/` pages, meta/OG/manifest names, config defaults that surface, seeded data that renders. Framework-internal "laravel" references (paths, packages) out of scope — user-visible branding only. Layouts, manifests, robots, vendor mail themes, site pages, subjects all clean. %<9920>
   3. [x] Fix what the sweep finds; anything non-trivial gets its own disposition here. All live leaks fixed by the APP_NAME root cause (also renames the session cookie `laravel-session` → `cryptozing-session`). Recorded, no change: `config/mail.php` from-address/name placeholder defaults stay generic (self-hoster-safe; real values live in env), invoice-form field placeholders are intentional examples. Observation: sign-offs could use `effectiveMailBrandName()` to honor per-user brands in the blank-name edge case. %<9921>
   4. [x] [User] Eyes-on: help-page meta description and a client mail's sign-off read CryptoZing; spot-check any other surfaces the sweep touched. Verified. %<9922>

## Exit Criteria

- [x] ToS drafted with entity name CryptoZing LLC; self-reviewed; ready for publication at MS21 deploy time (effective date + Privacy Policy link resolve then). %<9924>
- [x] Privacy Policy drafted with entity name CryptoZing LLC; self-reviewed; ready for publication at MS21 deploy time (effective date + Terms of Service links resolve then). %<9925>
- [x] Disclaimer copy present at signup, wallet onboarding, and invoice/payment surfaces (entity-agnostic where possible; placeholder where not). %<9926>
- [x] Footer ToS / Privacy Policy link scaffolding in place across all pages; final wiring to live URLs activates at MS21 deploy. %<9927>
- [x] All UI and mail copy reviewed for monetization-neutral language; issues resolved. %<9928>
- [x] Monetization-safe language guide section added to `UX_GUARDRAILS.md`. %<9929>
