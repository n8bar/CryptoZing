# M21 Phase 2 — Switch to the Apex and Open Beta

Status: Approved by n8 - 9/5/2026 21:14.
Parent: [M21](../milestones/21_OB_DEPLOYMENT.md)
Prerequisite: [M21.1](x21.1_PRE_DEPLOY_VERIFICATION.md) complete.
Next: [M21.3 — Verify and sign off](21.3_ROLLOUT_VERIFICATION_SIGNOFF.md)

Goal: turn the existing alpha deployment into the public beta and retire its old hostname.

Cutover is serial. Public-response checks can be delegated after each change; production configuration has one owner. Normal processing continues through DNS preparation. The mail circuit breaker is not a pause mechanism: it records invoice-send attempts as skipped.

## 1. [x] Prepare the cutover and publication

1. [x] [User] Accept the cutover window and Phase 1's old-link disposition. Window: 2026-09-06, starting from the Phase 1 close; old alpha links may break, no replacements.
2. [x] Capture the current app/site image digests and nginx/env/DNS/webhook configuration in local recovery storage. `.cybercreek/ms21-phase2/pre-cutover/`: app `9cdb452` (`sha256:9c4cfe38…`), site `b65c063` (`sha256:4ab422f7…`), env, compose, DNS, Mailgun webhooks (all three → alpha).
3. [x] Take a fresh encrypted database backup and verify it decrypts using the [backup procedure](../ops/RUNNING_THE_SERVER.md#backups). `cz-2026-09-06_0249.sql.gz.age` decrypts on AL9: 17 tables, ephemeral tables schema-only.
4. [x] Put the actual publication date in the reviewed legal copies and the sitemap's root `lastmod`. Effective: September 6, 2026 in both policy copies and their `docs/legal/` drafts; sitemap root `lastmod` 2026-09-06 ([cryptozing-site#2](https://github.com/n8bar/cryptozing-site/pull/2)).
   1. [x] Publish the resulting app/site artifacts and record their immutable release IDs. App `d30da9525567a20783f9029a710d0cbc498d99ab` (built by manual dispatch; the Markdown-only merge skips the auto build), site `039efaa7c94ccd2e1232131a4becb9f7a8d33e19`.
   2. [x] Confirm Phase 1's tested configuration still applies. Template sets and `.env` keys unchanged since the §3.5 test.

## 2. [x] Make the apex ready on the existing stack

Normal apex traffic still goes to Pages; alpha continues serving the existing app.

1. [x] Issue and verify the apex/`www` certificate through the prepared DNS challenge. Issued in Phase 1 (lineage `cryptozing.app`: apex, `www`, `stats`; valid to 2026-12-05); serving on `stats.` already.
2. [x] Install the prepared transition routing and selected app/site release on the existing stack. `CZ_NGINX_TEMPLATES=templates-transition`; `publish-site.sh 039efaa…`; `deploy.sh d30da95…` (nothing to migrate); all services healthy.
   1. [x] Use the [existing deploy path](../../scripts/deploy.sh) with Phase 1's explicit site-image selection; migrations run before app services switch.
   2. [x] Keep `APP_URL`/`APP_PUBLIC_URL` on alpha and the approval gate enabled at this stage; retain existing wallet and mail settings. Read back before the install: alpha URLs, gate on, mainnet, aliasing off.
3. [x] Test apex HTTPS and HTTP/`www` redirects directly against the VPS using the intended hostnames and SNI. Apex 200; http and `www` 301 to the apex in one hop.
4. [x] Verify the landing page, published policies, and representative article/asset routes through that direct route. Landing title, `/terms` and `/privacy` with "Effective: September 6, 2026", `/learn/` + custody article, `/staging/`, sitemap (root `lastmod` 2026-09-06), robots, IndexNow key, og-preview, favicon, stats heartbeat.
5. [x] Verify alpha still serves the running app and its callbacks during the transition. `/up` and `/login` 200 from outside; `/webhooks/mailgun` route answers (405 to GET).

## 3. [ ] Move apex traffic

1. [x] Point apex/`www` DNS at the VPS, preserving mail and verification records. Apex A → `192.255.230.151`, `www` CNAME → apex; MX, SPF, verification, `_dmarc`, and `mailer.*` unchanged at the authoritative servers.
   1. [x] [Guided User] Complete required DNS-console changes. Namecheap; the apex A record was briefly absent after the GitHub records were removed and was re-added within minutes.
2. [ ] Verify authoritative and independent recursive responses, including any AAAA record, and allow the previous TTL to clear.
3. [x] Verify HTTPS, canonical redirects, landing/policy pages, and representative content through normal public DNS. Via Cloudflare's answer: apex 200 with the apex certificate, http and `www` 301 to the apex, `/terms`, `/privacy`, `/learn/`, an article, sitemap, robots, og-preview all 200; no `X-Robots-Tag` on the apex.
4. [x] Apply the new certificate's prepared unattended-renewal settings to the existing certificate infrastructure. `cryptozing.app.conf`: `authenticator = webroot`, `webroot_path = /opt/cryptozing/certbot-webroot`, manual keys removed (backup `.bak-manual`).
   1. [x] Test renewal and nginx reload for the apex while nginx owns port 80. `certbot renew --cert-name cryptozing.app --dry-run --run-deploy-hooks` succeeded; challenges served 200 from the webroot on all three names; `cz-nginx` reloaded in place.
5. [x] Confirm Pages is no longer the apex DNS target; retain its source/build configuration for recovery. Authoritative apex answer is the VPS; `pages.yml`, `CNAME`, and the Pages site remain for recovery.
   1. [x] Activate the prepared VPS content-publishing procedure. Content CLAUDE.md documents `scripts/publish-vps.sh` as the path ([cryptozing-site#3](https://github.com/n8bar/cryptozing-site/pull/3)).
   2. [x] Disable Pages-triggered IndexNow notifications. Step removed from `pages.yml` (same PR).
   3. [x] Update [CONTENT_PLAN.md](../CONTENT_PLAN.md) to match the new public serving target. Stage, publish, and index steps run through `publish-vps.sh`.

## 4. [ ] Switch generated links and callbacks

1. [x] Retarget Mailgun's `delivered`, `temporary_fail`, and `permanent_fail` registrations to `https://cryptozing.app/webhooks/mailgun`. Updated via the API from the box; read back all three on the apex.
   1. [x] Verify the apex callback accepts valid provider events without authentication/approval-gate interference and retains signature validation. Unsigned post → 403 `Invalid signature`; event signed with the real key on the box → 200 `ok`, gate on.
2. [x] Set `APP_URL=https://cryptozing.app`, `APP_PUBLIC_URL` to the same value, and `CZ_SERVER_NAME=cryptozing.app`. `.env.bak-pre-apex` kept on the box; `APP_PUBLIC_URL` follows `APP_URL`.
   1. [x] Recreate app, queue, scheduler, and affected nginx configuration; verify each holds its intended values. Keep the approval gate enabled for the smoke. All three app services and nginx recreated and healthy; each reports the apex URLs, gate on, mainnet, aliasing off; the landing canonical is the apex.
3. [ ] Sign in with an existing operator account on the apex and verify access to its existing invoices.
4. [ ] Send one operator-addressed invoice with the normal mail settings.
   1. [ ] Verify apex links and the corresponding provider callback/delivery state; retain the message for Phase 3's inbox check.
5. [x] Confirm Phase 1's old-link disposition is satisfied, including any required replacements. Old alpha links may break; no replacements required.
6. [x] Resolve outstanding old-host callbacks through the prepared completion/reconciliation procedure. Re-counted after the retarget: 13 `sent`, 4 `skipped`, none in flight; nothing to reconcile.

## 5. [ ] Retire alpha and open access

1. [ ] Remove alpha's DNS records and activate the prepared apex-only nginx configuration.
   1. [ ] Verify alpha no longer reaches the app, including wildcard DNS and cached-host requests.
   2. [ ] Retire alpha-only certificate/renewal entries after confirming the apex does not depend on them.
2. [ ] Set `ALPHA_GATE_ENABLED=false` and recreate app, queue, and scheduler.
3. [ ] Verify the apex remains reachable and the running services hold the final public configuration.
4. [ ] Hand Phase 3 the deployed IDs, cutover time, mail-test reference, and any remaining findings.

## Backout

A failed cutover check stops progression. Before opening access, retain the approval gate and restore the affected image/configuration/DNS values from §1. Do not reset the database for a hostname failure. Cached DNS may still reach the VPS after reversal, so keep it serving valid routes during recovery.

After opening, re-enable approval if the release must be withdrawn; preserve any newly created customer data during recovery.

For an actual payment/data incident, use the [halt procedure](../ops/RUNNING_THE_SERVER.md#halt-procedure); it is not the normal cutover sequence.

## Exit Criteria

- [ ] The same deployment serves the apex with valid TLS, working generated links/callbacks, and public access enabled.
- [ ] Alpha's hostname is retired and Pages is no longer the public serving target.
- [ ] Publication is live, recovery remains available, and Phase 3 has the cutover evidence.
