# MS19 Phase 9 Strategy — Two-Factor Authentication

Status: In progress.
Parent milestone doc: [`docs/milestones/19_OB_HARDENING_OPS.md`](../milestones/19_OB_HARDENING_OPS.md)

**Goal:** Add 2FA capability for open beta. Email-based 2FA is the baseline (mandatory deliverable). TOTP / authenticator-app 2FA is an opportunistic add — included if Phase 9 begins with meaningful time remaining before MS19 close, otherwise deferred to the 2028 release. A lightweight recommendation surface points users without 2FA toward enabling it.

This phase is **positionally last by design** — if additional phases are ever added to MS19, 2FA stays at the end of the rollup so it can ship on the latest feedback or be deferred most cleanly.

## Decisions made

- **TOTP cutoff trigger:** If Phase 9 begins with at least 3 working days remaining before MS19 close, attempt TOTP. Otherwise email-only and defer TOTP to the 2028 release.
- **Recovery for email 2FA:** Option A — recovery codes. Generate ~10 single-use codes at 2FA enable time, display once, store hashed, single-use. User saves them out-of-band (password manager, paper, etc.). If both email access AND all recovery codes are lost, account recreation is the answer (documented honestly in user help). No SMS fallback — NIST-deprecated as 2FA, requires paid vendor, weak against SIM-swap.
- **Code TTL and attempt lockout:** 10-minute TTL on the 6-digit code; 5 failed attempts before account locks for 15 minutes.
- **Recommendation surface:** Non-blocking dashboard banner only. No settings-page hint or soft-block modal — keep beta UX un-naggy.

## 1. Email 2FA implementation

1. [ ] Add 2FA toggle in user settings (opt-in, default off).
   1. Schema: nullable `two_factor_email_enabled_at` on `users`; enabled = non-null (timestamp doubles as audit).
2. [ ] On enable: send a verification code, require correct entry before enable takes effect (round-trip verification).
3. [ ] At login when 2FA is enabled: prompt for 6-digit code; send via the existing mail surface; 10-minute TTL.
   1. Hook: after Breeze's password check passes, don't log in — stash the pending user id + remember-me flag in the session and redirect to a guest-safe `/two-factor/challenge` page; the session guard logs in only after a valid code, with session regeneration on success (same fixation protection as password login).
   2. Codes: `random_int`-generated 6-digit, single active code per user, stored hashed on `users` (`two_factor_code_hash`, `two_factor_code_expires_at`); reissue overwrites.
4. [ ] Rate-limit code send/resend (max ~3 sends per 10 minutes).
   1. `RateLimiter` keyed per user for send/resend; the challenge route also carries a per-IP throttle against distributed guessing.
5. [ ] Lockout after 5 failed attempts; unlock after a 15-minute window.
   1. `two_factor_attempts` counter + `two_factor_locked_until` on `users`; the challenge page reports the remaining wait honestly.
6. [ ] Disable-2FA flow in user settings — accept either a current 6-digit code OR a recovery code as re-verification before disable.

## 2. Recovery flow (Option A — recovery codes)

1. [ ] On 2FA enable: generate ~10 single-use recovery codes; display them ONCE with strong UX nudge ("save these now; you won't see them again — paste into your password manager or print").
   1. Ten codes via `random_int`, formatted `XXXX-XXXX` for typability.
2. [ ] Store recovery codes hashed (one-way hash, same approach as password storage; never plaintext).
   1. Array of hashes in a `two_factor_recovery_codes` JSON column on `users` — Fortify's column shape, but hashed rather than encrypted; display-once means nothing needs decrypting later.
3. [ ] At login when 2FA is enabled: provide a "use recovery code" link/option alongside the email-code prompt. Each code is single-use; consume on successful match.
   1. Challenge page carries a "use a recovery code" alternate input; a match consumes the code by deleting its hash from the array, then completes login with the same session regeneration as the code path. Attempts count against the same lockout.
4. [ ] Allow user to regenerate recovery codes from settings — invalidates the old set, generates a new set, displays once.
   1. Regenerate mirrors the disable flow's re-verification (current code or a recovery code) before invalidating the old set — a hijacked session must not mint itself fresh codes.
5. [ ] Document in user-facing help: "Save your recovery codes when you set up 2FA. If you lose both email access AND your recovery codes, account recreation will be required — there is no further recovery path."

## 3. Recommendation surface

1. [ ] Add a non-blocking dashboard banner for users without 2FA enabled: short copy + "Enable 2FA" CTA linking to settings.
   1. A Blade partial on the dashboard, shown when the user has no 2FA enabled (email or TOTP); copy stays un-naggy per Decisions, CTA links to the settings section.
2. [ ] Banner dismisses for the session when clicked or X-ed; reappears next session if 2FA still off.
   1. Dismissal sets a session key — no schema, no persistence; it reappears next session by design, so nothing to store or migrate.
3. [ ] Banner suppresses entirely once 2FA is enabled.
   1. Suppression is the enabled-check in 3.1 — once `two_factor_email_enabled_at` (or the TOTP equivalent) is non-null, the partial never renders.

## 4. TOTP (opportunistic — gated by Decisions §1)

Triggered only if cutoff met (≥3 working days remaining at phase start). Skip entirely if not — defer to the 2028 release.

1. [ ] Pull `pragmarx/google2fa` (or equivalent) Composer package.
   1. `pragmarx/google2fa` for secret generation/verification; the QR renders the `otpauth://` URI through the already-shipped SimpleSoftwareIO QrCode facade — no new QR dependency.
2. [ ] Enable flow: generate secret, display QR, verify with submitted code before enable takes effect.
   1. Secret on `users` in `two_factor_totp_secret` with an `encrypted` cast — the server must read it back at every login to verify, so encryption at rest, not hashing.
   2. Two-step confirm: secret stored pending (`two_factor_totp_confirmed_at` null), enrollment screen shown, one valid app code required before the timestamp is set — an unconfirmed secret never gates login.
   3. Below the QR, a "can't scan? set up manually" disclosure exposes the same enrollment data in the formats authenticators ask for: the full `otpauth://` URI as copyable text, and the raw base32 secret with account label and "time-based" noted for manual entry. Both copyable — no hand-converting between formats.
3. [ ] Login flow: prompt for TOTP code instead of email code if the user has TOTP enabled.
   1. Same challenge page and pending-login session as §1; the server picks the prompt (app code vs. emailed code) from the user's enabled method. Attempts and lockout share §1's counters. Verification accepts one window (±30s) of clock drift.
4. [ ] Recovery codes for TOTP: generate at enable, display once, store hashed, single-use. (TOTP recovery requires recovery codes — "contact maintainer" doesn't help if the authenticator device is lost.)
5. [ ] Allow users to switch between email 2FA and TOTP in settings.

## 5. Tests

1. [ ] Feature test: email 2FA enable round-trip works.
2. [ ] Feature test: login with email 2FA — happy path + invalid code + expired code + lockout.
3. [ ] Feature test: rate limiting on resend.
4. [ ] Feature test: recovery code login path — valid code logs in + consumes the code (subsequent reuse rejected); invalid code rejected; all codes consumed = no more recovery available.
5. [ ] Feature test: regenerate recovery codes — old set invalidated, new set displayed, login works against new set only.
6. [ ] Feature test: banner display for users without 2FA; suppression once enabled.
7. [ ] If TOTP shipped: feature tests for TOTP enable / login / recovery code use / disable / switch.
8. [ ] Feature test: xpub change demands step-up — rejected without password; rejected with password but bad/missing 2FA code when 2FA is enabled; succeeds with both.

## 6. Omissions and Corrections

Items that should already be in the app. They ride this arc because they need doing now — fit with the 2FA scope is not a requirement.

1. [ ] Require step-up re-verification to change the wallet xpub at `/wallet/settings`: current password always, plus a valid 2FA code when the user has 2FA enabled. A hijacked browser session must not be able to repoint future invoice payments at a foreign xpub.

## Exit Criteria

- [ ] Email 2FA available as opt-in user setting; full enable / login / disable flow works with rate limiting and lockout per Decisions §3.
- [ ] Recovery codes generated at enable, displayed once, stored hashed, single-use; regenerate flow available in settings.
- [ ] User-facing help documents the recovery story honestly: codes are the rescue; lost-all-factors = account recreation.
- [ ] Non-blocking dashboard banner ships for users without 2FA enabled.
- [ ] TOTP shipped if cutoff met; otherwise explicitly deferred to the 2028 release with the deferral noted at MS19 close.
- [ ] Wallet xpub changes require step-up re-verification per §6.
- [ ] Tests cover all flows in scope (including recovery code use, regenerate, exhausted codes).
