# MS19 Phase 6 Strategy — Content Promises Reconciliation

Status: Complete — all majors dispositioned, minors bulk-verified, catalog hygiene confirmed.
Parent milestone doc: [`docs/milestones/x19_OB_HARDENING_OPS.md`](../milestones/x19_OB_HARDENING_OPS.md)

**Goal:** Walk every open entry in [`docs/CONTENT_PROMISES.md`](../CONTENT_PROMISES.md) against the finished product. Confirm honored, revise the content, or revise the product — no open promises ship past open beta.

## Approach

Per-case judgment. No pre-set lean toward content vs. product revision — each entry gets evaluated on its own merits against the product's documented goals, philosophies, and principles. Thorough check on majors; bulk pass on minors with low-consequence drift routed to backlog rather than elevated.

## 1. Walk major entries

1. [x] Iterate through major entries in `CONTENT_PROMISES.md` (catalog order unless a different sequence emerges).
   1. [x] Major 1 — "taking a cut they expect you to absorb" hypocrisy risk. Deferred — monetization-gated, parked as [`BACKLOG.md`](../BACKLOG.md) Monetization item 23.
   2. [x] Major 2 — 0% fee during beta (comparison table, article copy, video wrap-up); product-verified fee-free, copy scoped to beta. Honored as-is.
   3. [x] Major 3 — stated current-limitations list (no Lightning, no CSV/JSON export, no recurring invoices, no QuickBooks, basic client management, dashboard-only reporting). Honored — accurate today; living-article obligation dropped for a takedown policy (article comes down if it becomes incorrect; replacement planned or not then).
   4. [x] Major 4 — public invoice recomputes BTC at the current rate on each open; no invalidating expiry (video claims). Honored — code-verified (`BtcRate::fresh()` per request, BTC recomputed from USD at render; no rate lock, no auto-expiry; issuer-set share-link expiry gates the URL only, not the invoice).
2. [x] For each entry: read source, walk product behavior, decide honored / content revised / product revised. 1 deferred (monetization-gated), 3 honored.
3. [x] Update catalog status as each entry is resolved.

## 2. Bulk-verify minor entries

1. [x] Single sweep over minor entries confirming the product still does what it does. All 19 hold — subagent code-walk with per-entry evidence (routes, services, views); stale `site/index.html` line refs corrected in the catalog.
2. [x] Flag drift but don't elevate low-consequence items — route to backlog if low-stakes / high-effort. No drift surfaced.

## 3. Resolve drift cases

1. [x] Each surfaced drift case gets per-case resolution: content revision, product revision, or backlog deferral. No drift cases surfaced.
2. [x] Update catalog status to reflect resolution. Nothing to update.

## 4. Catalog hygiene

1. [x] Confirm every open entry has a final status (honored / content revised / product revised / deferred). Majors: 3 honored, 1 deferred; minors carry the bulk-verified note (abbreviated format has no per-entry status).
2. [x] Update [`DELETED_PROMISES.md`](../DELETED_PROMISES.md) for any entries removed as part of resolution. No entries removed.

## 5. Wallet gap-limit pitfall

1. [x] Publish user-facing help/FAQ content for [#123](https://github.com/n8bar/CryptoZing/issues/123): why a payment CryptoZing shows as paid may not appear in the merchant's wallet (gap limit), that the funds are intact, and how to resolve (raise gap limit / rescan). Landed in Phase 5's open-issue sweep (`/help#gap-limit`, [`x19.5`](x19.5_LEGAL_LAYER.md) §7.7); #123 closed.

## Exit Criteria

- [x] Every major catalog entry has a final status.
- [x] Minor entries bulk-verified; any drift handled or backlogged.
- [x] No open entries remain at Phase 6 close.
- [x] Gap-limit pitfall content published ([#123](https://github.com/n8bar/CryptoZing/issues/123)). `/help#gap-limit`, via Phase 5.
