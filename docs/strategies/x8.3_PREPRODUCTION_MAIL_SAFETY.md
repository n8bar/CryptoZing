# MS8 Phase 3 Strategy - Pre-Production Mail Safety

Status: Complete (retrospective reconstruction).
Parent milestone: [`docs/milestones/x8_INVOICE_DELIVERY_AUTO_RECEIPTS.md`](../milestones/x8_INVOICE_DELIVERY_AUTO_RECEIPTS.md)
Historical execution date: 2025-11-17.

> This phase boundary was reconstructed on 2026-07-18 from the public-host, aliasing, configuration, and test portions of `dd8092e`. It was not named or checked off as Phase 8.3 during original execution.

## Goal

Make recipient-facing links environment-correct and prevent pre-production delivery tests from contacting real client addresses.

## Reconstruction basis

Primary evidence: `.env.example`, mail configuration, `MailAlias`, application binding, delivery-job integration, `MailAliasTest`, and the alias integration scenario added in `dd8092e`.

## 1. Separate public links from the runtime host

1. [x] Add `APP_PUBLIC_URL` with `APP_URL` as the local default. %<8072>
2. [x] Generate public invoice links from the explicit recipient-facing base. %<8073>
3. [x] Keep production configuration responsible for selecting `https://cryptozing.app`. %<8074>
4. [x] Avoid embedding an accidental localhost or staging host in outbound mail. %<8075>

## 2. Add configurable catch-all aliasing

1. [x] Add `MAIL_ALIAS_ENABLED` and `MAIL_ALIAS_DOMAIN` configuration. %<8077>
2. [x] Bind one configured `MailAlias` service. %<8078>
3. [x] Return the original address when aliasing is disabled or incomplete. %<8079>
4. [x] Normalize a valid source address into a catch-all local part. %<8080>
5. [x] Preserve malformed/non-email input rather than inventing a recipient. %<8081>
6. [x] Keep the behavior deterministic for To and CC recipients. %<8082>

## 3. Apply containment at the send boundary

1. [x] Resolve recipients from the persisted delivery row. %<8084>
2. [x] Convert the primary recipient immediately before constructing the outbound send. %<8085>
3. [x] Convert CC through the same service. %<8086>
4. [x] Leave the delivery record's original intended recipient intact for audit context. %<8087>
5. [x] Keep the mailable body and public invoice link unchanged by address rewriting. %<8088>

## 4. Verify enabled and disabled behavior

1. [x] Prove enabled aliasing rewrites a normal email into the configured catch-all domain. %<8090>
2. [x] Prove disabled aliasing returns the original address. %<8091>
3. [x] Prove whitespace/domain normalization. %<8092>
4. [x] Prove non-email input is returned unchanged. %<8093>
5. [x] Prove a real delivery job applies aliasing to both To and CC. %<8094>

## 5. Record the deployment boundary

1. [x] Document the pre-production catch-all intent in mail configuration. %<8096>
2. [x] Record the configured alias domain in historical PLAN/AGENTS guidance. %<8097>
3. [x] Make disabling aliasing an explicit production deployment requirement. %<8098>

## Phase Exit Criteria

- [x] Outbound public links use an explicit environment-appropriate base URL. %<8100>
- [x] Catch-all aliasing can contain both To and CC delivery in pre-production. %<8101>
- [x] Disabled aliasing preserves real recipients for eventual production use. %<8102>
- [x] Unit and feature tests cover the conversion and send-boundary integration. %<8103>
- [x] Production cutover owns the explicit alias-disable action. %<8104>

## Historical boundary

Aliasing was a containment mechanism, not delivery idempotency or a production circuit breaker. Later mail hardening added duplicate suppression, retries, send disablement, and truthful provider-state handling.
