# MS20 Phase 3 Strategy — Taproot Wallet Support

Status: Complete — PR [#143](https://github.com/n8bar/CryptoZing/pull/143) merged; alpha running the merge sha.
Parent milestone doc: [`docs/milestones/20_MAINNET_CUTOVER_ALPHA_GATE.md`](../milestones/20_MAINNET_CUTOVER_ALPHA_GATE.md)
Supporting spec: [`docs/specs/WALLET_XPUB_UX_SPEC.md`](../specs/WALLET_XPUB_UX_SPEC.md)

**Goal:** The watch-only wallet layer accepts Taproot (BIP86) account keys and derives bech32m (`bc1p`/`tb1p`) receive addresses for invoices and donations, so the Phase 4 operator wallets — and merchants at cutover — can be Taproot. Watch-only invariant unchanged: public keys in, addresses out, nothing that can sign.

## Decisions to confirm before flesh-out

1. Protocol coverage: Taproot (BIP86) only, or also other onboarding gaps surfaced by the same review (wrapped SegWit/BIP49, descriptor input, multisig)? _Answer:_ Taproot + descriptor input (`wpkh(...)`/`tr(...)`). Named exclusions, recorded in the spec: wrapped SegWit/BIP49, Silent Payments (BIP352 — detection needs a scan key, doesn't fit address-polling watch-only), Lightning (separate layer; sequenced after Ethereum support, which itself is deliberately not backlogged). Multisig stays in [`docs/BACKLOG.md`](../BACKLOG.md) §3.

## 1. [x] Spec first

1. [x] Extend `WALLET_XPUB_UX_SPEC.md`: supported script types, the bare-`xpub` ambiguity (same key string is a valid BIP84 or BIP86 account key, so onboarding must capture script type), and error/guidance copy. Landed as `## Key Formats & Script Types` + Goals line.
2. [x] [User] Approve the spec delta before implementation starts. Approved per-bullet; named exclusions dropped from the spec (scope lives in Decisions §1), bare keys default Native SegWit, key beats a contradicting form choice.

## 2. [x] Implementation

Rides `claude/taproot-wallet-support` + PR [#143](https://github.com/n8bar/CryptoZing/pull/143).

1. [x] Migration: script-type column (`bip84`|`bip86`) on the wallet key stores, defaulting `bip84`; donation wallet config carries the same.
2. [x] Descriptor intake at request validation: extract wrapper → script type + inner key (origins and `/0/*` tolerated); reject signing material before anything downstream sees the input. `WalletKeyInput::parse`, shared across the three requests via `NormalizesWalletKeyInput`.
3. [x] `derive-address.cjs` + `HdWallet`: script-type parameter, BIP86/`p2tr` derivation behind it; the node helper stays key-in-address-out. SLIP-132 prefix + `bip86` is a conflict error at the helper boundary too.
4. [x] `/wallet/settings` form per spec: script-type capture only for bare keys (Native SegWit default), preview in the saved type, contradiction error; request rules updated across `WalletSettingRequest`/`WalletAccountRequest`/`WalletKeyPreviewRequest`. Script-type change alone counts as a repoint → step-up.
5. [x] Script-type awareness in `WalletUnsupportedConfigurationDetector` and `WalletKeyLineage`. Fingerprint gains a script-type dimension; bip84 keeps the historic hash so existing cursors/invoices stay attached.
6. [x] `DONATION_WALLET_XPUB` path accepts the same inputs with the same semantics.
7. [x] Helper accordion: Taproot account export steps + descriptors-accepted line.
8. [x] Tests alongside each item; BIP86 address vectors from a reference wallet fixture. Vectors are the BIP86 spec's published m/86'/0'/0' addresses.

## 3. [x] Verification

1. [x] [Agent] Full local suite green (`./vendor/bin/sail artisan test`).
2. [x] Testnet4 onboarding proof:
   1. [x] [Agent] Onboard a BIP86 `tpub` and derive the first several `tb1p` addresses. Onboarded as a `tr(tpub…/0/*)` descriptor through the live `/wallet/settings` flow; stored `testnet4`/`bip86`; app-derived indexes 0–4 match the generated source wallet (`.cybercreek/ms20-phase3/`).
   2. [x] [User] Confirm they match the source wallet's receive addresses. Seed restored in Wasabi (Testnet4, Taproot); all five receive addresses match the app's, verified end to end.
3. [x] [Agent] Watcher detects a real Testnet4 payment to a `tb1p` invoice address; invoice transitions normally. Invoice 130 (`INV-0001`, taproot-qa): 10,000 sats to the index-0 `tb1p` address, detected unconfirmed, held until block 148196, then `paid` with `paid_at`; notification mails verified against the log driver.
4. [x] [Agent] BIP84 regression: an existing onboarded key derives identical addresses before and after. All 19 dev invoices with resolvable key lineage re-derive byte-identical on the branch (4 distinct historical QA keys); 2 lacked local ground-truth keys, none mismatched. Pre-existing BIP84 vectors in `HdWalletTest` untouched and green.
5. [x] [User] Eyes-on the settings flow against `docs/UX_GUARDRAILS.md`. Verdict: acceptable; likely to need refinement after usability testing. Covered saved-state rendering, helper accordion, descriptor paste (script-type choice removal), save round-trip.

## Exit Criteria

- [x] A Taproot account key onboards at `/wallet/settings` and derives correct bech32m addresses, verified against the source wallet. (§3.2)
- [x] Existing BIP84 (`zpub`/`vpub`/bare `xpub`) onboarding behavior is unchanged. (§3.1, §3.4)
- [x] Watch-only audit clean: no signing material anywhere in the new paths. Security-review pass found no exploitable paths; its two defense-in-depth notes (helper accepting private version bytes, SLIP-132 prv prefixes missing the signing-material error) fixed with tests.
- [x] Make sure alpha.cryptozing.app is running the latest developments from the dev environment. Deployed `6573f6e` (the #143 merge) via `scripts/deploy.sh`; site 200, `script_type` migration ran.
