# MS9 Phase 3 Strategy - Authoring UX & Verification

Status: Complete (retrospective reconstruction).
Parent milestone: [`docs/milestones/x9_PRINT_PUBLIC_POLISH.md`](../milestones/x9_PRINT_PUBLIC_POLISH.md)
Historical execution date: 2025-11-18.

> This phase boundary was reconstructed on 2026-07-18 from the final spec correction, branding-heading implementation, form refinement, tests, and completed PLAN move in `ee99787`, `c4280cf`, `d6845a2`, and `b9ac793`. It was not named or checked off as Phase 9.3 during original execution.

## Goal

Finish the owner-facing authoring controls, lock the intended output details, and close the print/public milestone under feature coverage.

## Reconstruction basis

Primary evidence: `ee99787`, `c4280cf`, `d6845a2`, and `b9ac793`, plus the completed form/output assertions in `InvoicePaymentDisplayTest` and `PublicShareTest`.

## 1. Add the customizable heading

1. [x] Add an owner-level branding heading. %<8296>
2. [x] Add an optional invoice-level heading override. %<8297>
3. [x] Resolve the effective heading with the same override-first rule as other billing fields. %<8298>
4. [x] Render the heading on invoice output. %<8299>
5. [x] Keep rich branding and logo upload out of scope. %<8300>

## 2. Refine create/edit authoring

1. [x] Group branding and footer overrides into one collapsible section. %<8302>
2. [x] Keep the main invoice fields visible without making optional branding dominate the form. %<8303>
3. [x] Use the owner defaults as context while preserving blank invoice overrides. %<8304>
4. [x] Keep create and edit controls symmetrical. %<8305>
5. [x] Preserve submitted override values across validation errors. %<8306>

## 3. Lock print/public details

1. [x] Standardize the QR render at the template-supported 180px size. %<8308>
2. [x] Keep the rate-refresh caption near the QR. %<8309>
3. [x] Keep contact/footer blocks from crowding payment summaries. %<8310>
4. [x] Preserve readable outstanding and unavailable messages. %<8311>
5. [x] Update the print/public spec to match the shipped heading and QR behavior. %<8312>

## 4. Verify the finished output

1. [x] Assert private print renders owner defaults and invoice overrides. %<8314>
2. [x] Assert authenticated show renders billing details and the footer. %<8315>
3. [x] Assert active public output renders branding details. %<8316>
4. [x] Preserve expired/disabled-state tests. %<8317>
5. [x] Keep payment history and outstanding-targeted QR tests passing. %<8318>

## 5. Close the milestone

1. [x] Move Print & Public Polish into historical PLAN's completed milestones. %<8320>
2. [x] Link the completed outcome to the print/public spec. %<8321>
3. [x] Leave logo upload, rich templates, multilingual templates, and broader UX restructuring to later work. %<8322>

## Phase Exit Criteria

- [x] Branding heading and billing fields support owner defaults plus invoice overrides. %<8324>
- [x] Optional authoring controls remain contained in a collapsible form section. %<8325>
- [x] Print and public output use the final MS9 QR and branding presentation. %<8326>
- [x] Feature tests cover private and public customizable output. %<8327>
- [x] Historical PLAN records MS9 complete. %<8328>

## Historical boundary

The milestone closed visual and data consistency, not a full browser/accessibility audit. Later UX work supplied broader responsive, dark-mode, focus, and shared-template verification.
