# MS7 Phase 1 Strategy - Payment Ledger & Partial Status

Status: Complete (retrospective reconstruction).
Parent milestone: [`docs/milestones/x7_PARTIAL_PAYMENTS_OUTSTANDING_SUMMARIES.md`](../milestones/x7_PARTIAL_PAYMENTS_OUTSTANDING_SUMMARIES.md)
Historical execution date: 2025-11-13.

> This phase boundary was reconstructed on 2026-07-18 around the ledger and state implementation in `b1ec935`. It was not named or checked off as Phase 7.1 during original execution.

## Goal

Represent each detected on-chain transaction independently, preserve its detection-time value, and derive partial/paid invoice state from the accumulated ledger.

## Reconstruction basis

Primary evidence: prospective spec commit `219e489`, its clarification commits through `805b3f9`, implementation commit `b1ec935`, and the contemporaneous watcher feature tests.

## 1. Define the payment-ledger contract

1. [x] Keep the invoice's original amount as the target while adding a one-to-many payment relationship. %<8112>
2. [x] Represent a payment with: %<8113>
   1. [x] invoice and transaction identity %<8114>
   2. [x] optional output index %<8115>
   3. [x] sats received %<8116>
   4. [x] detection and confirmation timestamps %<8117>
   5. [x] confirmed block height %<8118>
   6. [x] BTC/USD rate and fiat snapshot %<8119>
   7. [x] optional metadata %<8120>
3. [x] Prevent duplicate rows for the same invoice and transaction ID. %<8121>
4. [x] Add `partial` to the invoice lifecycle. %<8122>
5. [x] Preserve draft and void state from automatic partial-state changes. %<8123>

## 2. Add persistence and model support

1. [x] Create the `invoice_payments` table with a cascading invoice foreign key. %<8125>
2. [x] Add the `InvoicePayment` model with explicit fillable fields and casts. %<8126>
3. [x] Add invoice/payment relationships in both directions. %<8127>
4. [x] Add aggregate accessors for paid and outstanding sats. %<8128>
5. [x] Raise the original payment matching tolerance to 100 sats. %<8129>
6. [x] Add the initial significant-overpayment helpers without mutating ledger rows. %<8130>

## 3. Record every detected transaction

1. [x] Change detection from one full-payment result to a collection of address transactions. %<8132>
2. [x] Sum all outputs to the invoice address within each transaction. %<8133>
3. [x] Retain under-target transactions instead of ignoring them. %<8134>
4. [x] Upsert each transaction into the ledger by invoice and txid. %<8135>
5. [x] Persist detection, confirmation, block, sats, and rate metadata. %<8136>
6. [x] Calculate and store the payment's fiat value from the captured rate when available. %<8137>
7. [x] Keep repeated watcher runs idempotent for an existing transaction. %<8138>

## 4. Refresh invoice state from the ledger

1. [x] Sum ledger sats into the invoice-level payment summary. %<8140>
2. [x] Compare received sats to the original expected target with the 100-sat tolerance. %<8141>
3. [x] Mark an eligible under-target invoice `partial`. %<8142>
4. [x] Mark an eligible at-target invoice `paid` and set `paid_at` on the first crossing. %<8143>
5. [x] Leave draft and void invoices in their existing lifecycle state while still recording detected activity. %<8144>
6. [x] Save refreshed state after every watcher update. %<8145>

## 5. Verify and hand off

1. [x] Prove an under-target transaction creates a ledger row and partial state. %<8147>
2. [x] Preserve confirmation updates for an existing payment row. %<8148>
3. [x] Preserve the original unconfirmed-as-paid behavior in the contemporaneous full-target test. %<8149>
4. [x] Leave the ledger aggregates ready for the outstanding-summary presentation phase. %<8150>

## Phase Exit Criteria

- [x] `invoice_payments` stores distinct detected transactions without duplicates. %<8152>
- [x] Each row can preserve sats, confirmation metadata, and a USD snapshot. %<8153>
- [x] Invoice paid/outstanding sats derive from the ledger. %<8154>
- [x] Eligible invoices can transition to `partial` or `paid` after watcher updates. %<8155>
- [x] Faked-API watcher coverage proves the first ledger/state behaviors. %<8156>

## Historical boundary

This phase counted unconfirmed sats toward settlement and used sat-based target comparison. MS12 later made confirmed per-payment USD the settlement authority and introduced `pending` state plus dropped-unconfirmed cleanup.
