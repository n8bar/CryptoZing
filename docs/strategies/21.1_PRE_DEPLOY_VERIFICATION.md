# M21 Phase 1 — Prepare the In-place Release

Status: Draft for review — execution not started.
Parent: [M21](../milestones/21_OB_DEPLOYMENT.md)
Next: [M21.2 — Switch to the apex](21.2_DEPLOY_AND_CUTOVER.md)

Goal: prepare what changes when the existing deployment becomes public. The milestone owns the [accepted baseline and scope](../milestones/21_OB_DEPLOYMENT.md#accepted-baseline).

Parallel work: content-repo navigation and analytics can proceed alongside app landing/nginx work. One owner integrates deployment configuration; policy wording follows the analytics outcome.

## 1. [ ] Prepare public serving

1. [ ] Capture the currently published URL baseline from the content repo and Pages: routes, redirects, metadata, assets, sitemap, and verification files.
2. [ ] Prepare the existing deployment overlay/nginx for temporary alpha-plus-apex serving, then final apex-only serving.
   1. [ ] Route `/` and policy URLs to Laravel; preserve `/learn/*`, existing `/staging/*` review URLs, slash redirects, and content-owned assets/support files through the site container.
   2. [ ] Serve the content sitemap and webmaster/IndexNow verification files; preserve crawlability without removing invoice or staging noindex.
   3. [ ] Preserve the existing ACME webroot and nginx reload hook; cover HTTPS and HTTP/`www` canonical redirects.
   4. [ ] Make the final configuration reject the retired alpha hostname, including requests using cached DNS.
3. [ ] Give the site image an explicit SHA/digest in our overlay; it currently uses `latest`.
   1. [ ] Verify app deploys and site-only updates use the selected artifacts.
   2. [ ] Verify rollback restores the prior selection.
4. [ ] Bring [the Laravel landing page](../../resources/views/welcome.blade.php) up to the publication baseline.
   1. [ ] Add the missing description, canonical/social metadata, structured data, and matching public assets.
   2. [ ] Add the legal footer required by [M19.5](x19.5_LEGAL_LAYER.md) and a working route to the guides.
5. [ ] Remove the content repo's GitHub nav link, retaining its footer link.
6. [ ] Prepare the content repo's site-only VPS publish/rollback procedure with IndexNow notification after successful VPS publication; activation belongs to M21.2 §3.
7. [ ] Verify changed landing/navigation surfaces against the [UX guardrails](../UX_GUARDRAILS.md).

## 2. [ ] Prepare analytics and legal publication

Umami migration remains non-gating under the milestone's scope decision. A deferral skips the migration steps and retains the actual provider disclosure.

1. [ ] Assess capacity for isolated self-hosted Umami and its database, TLS, and backups.
2. [ ] Deploy and verify the analytics service.
3. [ ] Repoint the content repo's `_layouts/default.njk` integration; verify a published article event arrives without duplicate Cloud collection.
4. [ ] Record the analytics outcome or a named deferral before preparing policy edits.
5. [ ] Preserve the replacement landing page's marketing measurement using deployment-specific configuration; verify it reaches the selected endpoint without enabling tracking for generic self-hosters.
6. [ ] Verify staging, signed-in app, and invoice pages remain outside analytics.
7. [ ] Prepare the publication delta in `resources/markdown/terms.md`, `resources/markdown/privacy.md`, and their `docs/legal/` counterparts.
   1. [ ] Match Privacy Policy §§2/7 to the analytics outcome; Phase 2 supplies the actual effective date.
   2. [ ] [User] Review changed policy wording and the new landing-page presentation. Existing disclaimer placements retain M19.5's approval.

## 3. [ ] Prepare the hostname transition

1. [ ] Capture apex/`www`/alpha DNS records and TTLs, including mail and verification records that must remain unchanged.
2. [ ] Prepare apex/`www` certificate issuance before web DNS moves, using a DNS challenge; arrange unattended renewal through the existing certificate infrastructure.
   1. [ ] [Guided User] Complete any DNS-account access needed for issuance.
3. [ ] [User] Decide whether old alpha email/share links may stop working when the hostname is removed or need replacement apex links first.
4. [ ] Identify outstanding alpha-host mail callbacks and prepare their completion/reconciliation before hostname removal. Preserve the existing provider account and signing key.
5. [ ] Test the prepared routing and deploy commands for both transition and final configurations, including return to the current working configuration.
6. [ ] Verify our overlay changes preserve the generic self-host boundary against [M20.2's clean-install proof](x20.2_PRODUCTION_HOSTING.md).
   1. [ ] Test any changed shared installation/startup paths using the [self-host guide](../ops/get-live/README.md); unchanged paths retain the existing evidence.
7. [ ] Record passing app/site release IDs and the prepared configuration changes here for Phase 2.

## Exit Criteria

- [ ] Apex serving, landing/content changes, and hostname retirement are prepared and tested.
- [ ] Analytics has a disposition and the legal publication delta is reviewed.
- [ ] Phase 2 has executable certificate, deployment, and backout steps for the selected artifacts; no unresolved preparation blocker remains.
