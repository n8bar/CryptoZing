# M21 Phase 1 — Prepare the In-place Release

Status: Approved by n8 - 9/5/2026 21:11.
Parent: [M21](../milestones/21_OB_DEPLOYMENT.md)
Next: [M21.2 — Switch to the apex](21.2_DEPLOY_AND_CUTOVER.md)

Goal: prepare what changes when the existing deployment becomes public. The milestone owns the [accepted baseline and scope](../milestones/21_OB_DEPLOYMENT.md#accepted-baseline).

Parallel work: content-repo navigation and analytics can proceed alongside app landing/nginx work. One owner integrates deployment configuration; policy wording follows the analytics outcome.

## 1. [x] Prepare public serving

1. [x] Capture the currently published URL baseline from the content repo and Pages: routes, redirects, metadata, assets, sitemap, and verification files. Snapshot: `.cybercreek/ms21-phase1/url-baseline-2026-09-05.md`. 9 sitemap URLs, 8 noindexed `/staging/*`, 14 support files; slash 301s; `www`/http → apex 301; `/terms` and `/privacy` 404 on Pages though the content footer links them (Laravel owns them after cutover); no custom 404.
2. [x] Prepare the existing deployment overlay/nginx for temporary alpha-plus-apex serving, then final apex-only serving. `CZ_NGINX_TEMPLATES` selects `templates-alpha` (default) | `templates-transition` | `templates-apex` in `compose.alpha.yaml`; both new sets pass `nginx -t` and a local routing smoke against a stub site container.
   1. [x] Route `/` and policy URLs to Laravel; preserve `/learn/*`, existing `/staging/*` review URLs, slash redirects, and content-owned assets/support files through the site container. `/`, `/terms`, `/privacy` → app; `^~ /learn/`, `^~ /staging/` → site; `/learn` and `/staging` 301 to the slash form; slashless articles get the site container's relative 301.
   2. [x] Serve the content sitemap and webmaster/IndexNow verification files; preserve crawlability without removing invoice or staging noindex. `= /robots.txt`, `= /sitemap.xml`, and `^/[0-9a-f]{32}\.txt$` → site; the apex adds no `X-Robots-Tag` (invoice views set their own header; staging is noindexed in-page).
   3. [x] Preserve the existing ACME webroot and nginx reload hook; cover HTTPS and HTTP/`www` canonical redirects. ACME webroot on every port-80 name; `cz-nginx` container name kept for the reload hook; http → https, `www` → apex in one hop.
   4. [x] Make the final configuration reject the retired alpha hostname, including requests using cached DNS. `templates-apex` adds `default_server` blocks on 80/443 returning 444; alpha-by-IP and raw-IP requests dropped in the smoke.
3. [x] Give the site image an explicit SHA/digest in our overlay; it currently uses `latest`. `CZ_SITE_TAG` is required (no `latest` fallback), guarded by `DeploymentOverlayTest`; current release `b65c063b5e6aad51b6ed87e2dd47801353e80d35` (`sha256:4ab422f7…`); the box still runs the 2026-08-11 `latest` pull. `scripts/publish-site.sh <sha>` prints the previous tag as the rollback target; live proof of 1–2 rides §3.5.
   1. [x] Verify app deploys and site-only updates use the selected artifacts. Box rolled from the 2026-08-11 `latest` pull to `b65c063…` via `publish-site.sh`; alpha `/learn/` serves the custody article.
   2. [x] Verify rollback restores the prior selection. `publish-site.sh e536732…` removed the article, `publish-site.sh b65c063…` restored it; `.env` ends on the selected tag.
4. [x] Bring [the Laravel landing page](../../resources/views/welcome.blade.php) up to the publication baseline. `9a6ce77`; `LandingPageTest`.
   1. [x] Add the missing description, canonical/social metadata, structured data, and matching public assets. Title/description/canonical/og/twitter/JSON-LD derive from `app.public_url`; `og-preview.*` added to `public/`.
   2. [x] Add the legal footer required by [M19.5](x19.5_LEGAL_LAYER.md) and a working route to the guides. `x-legal-footer` (html gets `dark` for #128 contrast); Guides button → `app.guides_url` (`GUIDES_URL`, default the apex guides).
5. [x] Remove the content repo's GitHub nav link, retaining its footer link. [cryptozing-site#1](https://github.com/n8bar/cryptozing-site/pull/1).
6. [x] Prepare the content repo's site-only VPS publish/rollback procedure with IndexNow notification after successful VPS publication; activation belongs to M21.2 §3. `scripts/publish-vps.sh` (content repo, same PR) drives the box-side `scripts/publish-site.sh`; IndexNow only after the public sitemap matches the release; rollback is the same command with the previous sha.
7. [x] Verify changed landing/navigation surfaces against the [UX guardrails](../UX_GUARDRAILS.md). Guides button matches its neighbours (focus ring; row wraps on narrow widths); footer contrast via the dark palette.

## 2. [x] Prepare analytics and legal publication

Umami migration remains non-gating under the milestone's scope decision. A deferral skips the migration steps and retains the actual provider disclosure.

1. [x] Assess capacity for isolated self-hosted Umami and its database, TLS, and backups. Box: 3.6 GB RAM with 2.5 GB available, 49 GB disk free; Umami 3.3.1 + its own Postgres 16 volume need ~400 MB. TLS: `stats.cryptozing.app` rides the apex certificate lineage; nginx serves it noindexed in every template set. Backups: a `pg_dump` line joins the nightly `dump.sh` on the box, same age recipient.
2. [x] Deploy and verify the analytics service. Live at `stats.cryptozing.app` (Umami 3.3.1, own Postgres, heartbeat healthy, noindex, apex certificate); admin credentials and website ID in local storage; nightly `dump.sh` now dumps its database too.
3. [x] Repoint the content repo's `_layouts/default.njk` integration; verify a published article event arrives without duplicate Cloud collection. [cryptozing-site#1](https://github.com/n8bar/cryptozing-site/pull/1) merged; live pages load only the self-hosted script, and an article pageview was recorded through the front nginx.
4. [x] Record the analytics outcome or a named deferral before preparing policy edits. Outcome: self-hosted; Cloud collection ended with the content deploy.
5. [x] Preserve the replacement landing page's marketing measurement using deployment-specific configuration; verify it reaches the selected endpoint without enabling tracking for generic self-hosters. `ANALYTICS_SCRIPT_URL` + `ANALYTICS_WEBSITE_ID` (`config/analytics.php`); either blank loads nothing. Alpha's landing loads the stats script with the website ID; the endpoint records through the front nginx.
6. [x] Verify staging, signed-in app, and invoice pages remain outside analytics. Staging stays behind the content layout's noindex gate; only `welcome.blade.php` references the config (`LandingPageTest`), with `/terms`, `/privacy`, `/login`, and the dashboard asserted clean.
7. [x] Prepare the publication delta in `resources/markdown/terms.md`, `resources/markdown/privacy.md`, and their `docs/legal/` counterparts. Privacy §2/§7 reworded for first-party analytics in both copies; Terms unchanged (no analytics claim); effective dates stay `[date]` for M21.2 §1.4.
   1. [x] Match Privacy Policy §§2/7 to the analytics outcome; Phase 2 supplies the actual effective date. §2 and §7: "some of our sites count page views and record the page address, referrer, browser details, and a country derived from your IP" (§2 adds "but not the IP"); §9 reserves temporary extra logging, including IP addresses, during abuse or security investigations.
   2. [x] [User] Review changed policy wording. Approved as written in 1.
   3. [x] [User] Review the new landing-page presentation. Existing disclaimer placements retain M19.5's approval. Approved on alpha once [#180](https://github.com/n8bar/CryptoZing/pull/180) anchored the footer below the content.

## 3. [x] Prepare the hostname transition

1. [x] Capture apex/`www`/alpha DNS records and TTLs, including mail and verification records that must remain unchanged. Snapshot: `.cybercreek/ms21-phase1/dns-baseline-2026-09-05.md`. Namecheap BasicDNS; web TTLs 1800 s; no AAAA/CAA. Unchanged set: apex google-site-verification + SimpleLogin TXT/MX, `_dmarc`, and `mailer.` Mailgun MX/SPF/`k1` DKIM/`email.` CNAME/`_dmarc`.
2. [x] Prepare apex/`www` certificate issuance before web DNS moves, using a DNS challenge; arrange unattended renewal through the existing certificate infrastructure. Issued: lineage `cryptozing.app` with SANs `www.` and `stats.` via manual DNS-01 (Namecheap TXT). Renewal conf stays `manual` until M21.2 §3.4 switches it to `webroot` + `/opt/cryptozing/certbot-webroot`; the existing timer and `cz-nginx` reload hook then cover it.
   1. [x] [Guided User] Complete any DNS-account access needed for issuance. Namecheap console; `stats` A record added at the same time.
3. [x] [User] Decide whether old alpha email/share links may stop working when the hostname is removed or need replacement apex links first. May stop working; no replacement links (no customers, only our own validation/test mail carries them).
4. [x] Identify outstanding alpha-host mail callbacks and prepare their completion/reconciliation before hostname removal. Preserve the existing provider account and signing key. Prod `invoice_deliveries`: 13 `sent`, 4 `skipped`, none in flight (latest 2026-08-20). Reconciliation: re-run the status count immediately before M21.2 §5.1; any `queued`/`sending` row is settled by hand from Mailgun's event log.
5. [x] Test the prepared routing and deploy commands for both transition and final configurations, including return to the current working configuration. On the box via `CZ_NGINX_TEMPLATES` + `up -d --no-deps web`: transition serves apex/`www`/http/stats and the noindexed alpha together; apex-only drops alpha, `http://alpha`, and raw-IP; restored to `templates-alpha` with alpha serving. `/og-preview.png` on the apex waits on the `b715b7e` app deploy.
6. [x] Verify our overlay changes preserve the generic self-host boundary against [M20.2's clean-install proof](x20.2_PRODUCTION_HOSTING.md). Changes touch only `compose.alpha.yaml`, `templates-transition/`, `templates-apex/`, and `publish-site.sh`; the recipe's `compose.production.yaml`, `templates/`, and `deploy.sh` are untouched.
   1. [x] Test any changed shared installation/startup paths using the [self-host guide](../ops/get-live/README.md); unchanged paths retain the existing evidence. No shared path changed.
7. [x] Record passing app/site release IDs and the prepared configuration changes here for Phase 2. App `9cdb452` (deployed to alpha), site `b65c063b5e6aad51b6ed87e2dd47801353e80d35`; overlay, template sets, `publish-site.sh`, and the `.env` keys below are already on the box with `templates-alpha` active. Prepared `.env` additions: `CZ_SITE_TAG=b65c063b5e6aad51b6ed87e2dd47801353e80d35`, `CZ_NGINX_TEMPLATES=templates-transition` (`templates-apex` at M21.2 §5.1), `CZ_PUBLIC_SERVER_NAME=cryptozing.app`, `CZ_LEGACY_SERVER_NAME=alpha.cryptozing.app`; files reach the box by `scp` (no git there).

## Exit Criteria

- [ ] Apex serving, landing/content changes, and hostname retirement are prepared and tested.
- [ ] Analytics has a disposition and the legal publication delta is reviewed.
- [ ] Phase 2 has executable certificate, deployment, and backout steps for the selected artifacts; no unresolved preparation blocker remains.
