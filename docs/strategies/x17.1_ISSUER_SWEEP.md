# MS17 Phase 1 Strategy - "owner" → "issuer" Sweep

Status: Complete.
Parent milestone: [`docs/milestones/x17_PRODUCT_READINESS.md`](../milestones/x17_PRODUCT_READINESS.md)
Canonical inputs: [`docs/PRODUCT_SPEC.md`](../PRODUCT_SPEC.md)

Owner tags: `[Agent]` = critical-path code and doc work to drive directly. `[Subagent]` = safe delegated sidecar work that can run once prerequisites in this phase are done. `[User]` = work you can drive directly without specialized coaching.

The word "owner" is used throughout the codebase to mean the person who creates and sends invoices — the invoice issuer. This phase replaces it everywhere: UI copy, URLs, route names, code variables and method names, mail class names, mail template files, delivery type strings, database columns, and tests. The changelog is the only exception — historical entries are left as-is since they are a record of what was said at the time.

Two changes touch the database and require migrations:
- `invoices.last_past_due_owner_alert_at` column rename
- `invoice_deliveries.type` values that contain "owner" (stored string data)

Do these last, after all code changes are in place, so the migration and the code land together.

## Delivery type string rename map

These string values are stored in `invoice_deliveries.type` and referenced across services, jobs, and models. Every occurrence of the old value must be replaced with the new one in code before the migration runs.

| Old | New |
|---|---|
| `owner_paid_notice` | `issuer_paid_notice` |
| `payment_acknowledgment_owner` | `payment_acknowledgment_issuer` |
| `past_due_owner` | `past_due_issuer` |
| `owner_overpay_alert` | `issuer_overpay_alert` |
| `owner_underpay_alert` | `issuer_underpay_alert` |
| `owner_partial_warning` | `issuer_partial_warning` |

## Mail class and template rename map

| Old class | New class |
|---|---|
| `InvoiceOwnerPaidNoticeMail` | `InvoiceIssuerPaidNoticeMail` |
| `InvoicePaymentAcknowledgmentOwnerMail` | `InvoicePaymentAcknowledgmentIssuerMail` |
| `InvoicePastDueOwnerMail` | `InvoicePastDueIssuerMail` |
| `InvoiceOverpaymentOwnerMail` | `InvoiceOverpaymentIssuerMail` |
| `InvoiceUnderpaymentOwnerMail` | `InvoiceUnderpaymentIssuerMail` |
| `InvoicePartialWarningOwnerMail` | `InvoicePartialWarningIssuerMail` |

| Old template file | New template file |
|---|---|
| `mail/invoice-owner-paid.blade.php` | `mail/invoice-issuer-paid.blade.php` |
| `mail/invoice-payment-acknowledgment-owner.blade.php` | `mail/invoice-payment-acknowledgment-issuer.blade.php` |
| `mail/invoice-past-due-owner.blade.php` | `mail/invoice-past-due-issuer.blade.php` |
| `mail/invoice-overpayment-owner.blade.php` | `mail/invoice-overpayment-issuer.blade.php` |
| `mail/invoice-underpayment-owner.blade.php` | `mail/invoice-underpayment-issuer.blade.php` |
| `mail/invoice-partial-warning-owner.blade.php` | `mail/invoice-partial-warning-issuer.blade.php` |

## 1. Implementation (in order)

### 1. Routes and support UI %<9263>
1. [x] [Agent] In `routes/web.php`: rename URI segments `/support/owners/{owner}/...` → `/support/issuers/{issuer}/...` and route names `support.owners.*` → `support.issuers.*`. %<9264>
2. [x] [Agent] In `SupportDashboardController`: rename `$owners` → `$issuers`; update view data key. %<9265>
3. [x] [Agent] In `SupportInvoiceController`: rename parameter `User $owner` → `User $issuer`, variable `$owner` → `$issuer` throughout, and `authorizeSupportOwner()` → `authorizeSupportIssuer()`. %<9266>
4. [x] [Agent] In `SupportClientController`: same renames as above. %<9267>
5. [x] [Agent] In all support views (`support/dashboard`, `support/invoices/index`, `support/invoices/show`, `support/clients/index`, `support/clients/show`): rename `$owner`/`$owners` → `$issuer`/`$issuers` and update all `route('support.owners.*', ...)` calls to `route('support.issuers.*', ...)`. %<9268>

### 2. Mail classes %<9269>
1. [x] [Agent] Rename each mail class file and class name per the rename map above. %<9270>
2. [x] [Agent] Update the markdown view reference inside each class to the new template name. %<9271>

### 3. Mail templates %<9272>
1. [x] [Agent] Rename each blade template file per the rename map above. Content changes are not required unless the template copy itself contains the word "owner" — check each one. %<9273>

### 4. Delivery type strings in code %<9274>
1. [x] [Agent] `InvoiceDelivery` model: update the `statusLabel()` match and any label map entries. %<9275>
2. [x] [Agent] `InvoiceAlertService`: rename `$owner` variables → `$issuer`; replace all delivery type strings per the rename map. %<9276>
3. [x] [Agent] `InvoiceDeliveryService`: replace all delivery type strings in `preventsRepeatAfterSend()` and `sentDuplicateReason()`. %<9277>
4. [x] [Agent] `DeliverInvoiceMail` job: rename mail class references and replace all delivery type strings in the dispatch map, type arrays, and inline checks. %<9278>
5. [x] [Agent] Any other file that references these type strings by value — grep for each old string to confirm nothing is missed. %<9279>

### 5. Console command %<9280>
1. [x] [Agent] `SendPastDueInvoiceAlerts`: update the description string ("Email owners and clients..." → "Email issuers and clients..."). %<9281>

### 6. Tests %<9282>
1. [x] [Subagent] Rename `$owner` / `$owners` variables → `$issuer` / `$issuers` in all feature test files. %<9283>
2. [x] [Subagent] Update all `route('support.owners.*', ...)` calls to `route('support.issuers.*', ...)` in tests. %<9284>
3. [x] [Subagent] Update any delivery type string assertions to the new values. %<9285>

Affected test files: `SupportAccessTest`, `InvoiceNotificationTest`, `InvoiceDeliveryTest`, `InvoicePaymentCorrectionTest`, `TrashFlowsTest`, `GettingStartedFlowTest`, `AuthorizationTest`, `InvoiceRateTest`, `WatchPaymentsCommandTest`, `DashboardSnapshotTest`, `InvoicePaymentDisplayTest`, `PublicShareTest`, `InvoiceShowEditFlowTest`, `ClientShowRedirectTest`, `MailBrandingTest`, `UserSettingsTest`.

### 7. Other views %<9286>
1. [x] [Subagent] `invoices/edit.blade.php` line 195: update inline comment ("hidden from owner edit UX" → "hidden from issuer edit UX"). %<9287>
2. [x] [Subagent] Grep all remaining Blade views for any other "owner" copy that refers to the invoice issuer and fix inline. %<9288>

### 8. Migrations (do last) %<9289>
1. [x] [Agent] Write a migration to rename `invoices.last_past_due_owner_alert_at` → `invoices.last_past_due_issuer_alert_at`. Update `$fillable` and `$casts` in the `Invoice` model to match. %<9290>
2. [x] [Agent] Write a migration to update existing `invoice_deliveries.type` rows: replace each old value with the new value per the rename map. Use `DB::table()` updates, not Eloquent, to avoid model event side effects. %<9291>
3. [x] [Agent] Run `./vendor/bin/sail artisan migrate` and confirm both migrations apply cleanly. %<9292>

### 9. Run tests %<9293>
1. [x] [Agent] `./vendor/bin/sail artisan test` — full suite must pass. %<9294>

### 10. Commit and push %<9295>
1. [x] [Agent] Commit all changes together (code + migrations + doc update). %<9296>
2. [x] [Agent] Push and confirm CI passes. %<9297>

## 2. Verify Phase 1

1. [x] [Agent] All delivery type strings in code and in the database use the new `issuer_*` values. %<9306>
2. [x] [Agent] `invoices.last_past_due_issuer_alert_at` column exists; the old column name is gone. %<9307>
3. [x] [Agent] Mail classes and templates are renamed; old filenames are deleted. %<9308>
4. [x] [Agent] Support routes use `/issuers/{issuer}/` in URIs and `support.issuers.*` as route names. %<9309>
5. [x] [Agent] Full test suite passes. %<9310>
6. [x] [Subagent] No remaining `$owner` variables in controllers, services, jobs, or tests where `$issuer` is correct. %<9311>
7. [x] [User] Browser QA: %<9312>
   - [x] 1. Invoice list, detail, and edit pages show no "owner" in any visible label or copy. %<9313>
   - [x] 2. Dashboard headings and prompts use "issuer" where applicable. %<9314>
   - [x] 3. Support dashboard URL is `/support`, table copy says "issuer". %<9315>
   - [x] 4. Support issuer invoice and client URLs use `/issuers/` not `/owners/`. %<9316>
   - [x] 5. Mail preview copy does not say "owner" where "issuer" is correct. %<9317>
   - [x] 6. Check the browser address bar on each support page for any residual `/owners/` segment. %<9318>
